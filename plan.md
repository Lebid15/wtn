# plan — الخطة

**آخر تحديث:** 2026-08-02

| الجلسة | الأقسام | الموضوع |
|--------|---------|---------|
| 2026-07-31 | §1–§5 | التوجيه إلى المزوّدين · تحكّم المشغّل · السلسلة البديلة · الديون التقنية |
| 2026-08-01 | §6–§11 | إضافة الرصيد وطرق الدفع · العملات · إعدادات الوكيل · بنية الأقسام |
| **2026-08-02** | **§12–§17** | **واتساب: ربط الرقم · قالب الرسالة · الإرسال الفردي والجماعي · البوت** |
| **2026-08-02** | **§18–§21** | **الجرد النهائي: المصادر التلقائية · البنود اليدوية · اللقطات والربح** |

> **ابدأ غداً من §21 «ما تبقّى»** — وقبله §17 و§10 لبنود الجلسات السابقة.

---

## 1. مُنجَز ✅ — التوجيه إلى ZNET (المزوّد `alaya`)

المسار من ضغطة الوكيل حتى المزوّد الخارجي **مفتوح ومثبَت بالتجربة الحيّة**
على <https://wtn.onrender.com>. تفاصيل كل فحص في
[`package-path.md`](package-path.md).

### الدورات الثلاث المؤكَّدة

| الدورة | الفيش | النتيجة |
|--------|-------|---------|
| **الإلغاء** | `26073113565` | إرسال → `OK\|1\|` → بعد 12s `OK\|3\|` (IPTAL) → **ملغى + استرجاع تلقائي**. صافي أثر المحفظة = صفر |
| **النجاح** | `26073132942` | إرسال → `OK\|1\|` → بعد 24s `OK\|2\|هذا المنتج تم شحنه عالميا` → **ناجح** + وصلت ملاحظة المشغّل |
| **شراء الوكيل نفسه** | `26073180146` | من واجهة المتجر لا من لوحة الأدمن — مرّ من حماية الخسارة ووصل ZNET |

### ما بُني على الطريق

| # | البند | الحال |
|---|-------|-------|
| 1 | `ZnetAdapter` مطابق لوثيقة المزوّد الرسمية — محفوظة في [`docs/integrations/ZNET_API.md`](docs/integrations/ZNET_API.md) | ✅ |
| 2 | `oyun` + `kupur` معاً — ZNET يرقّم اللعبة بـ`oyun` والباقة داخلها بـ`kupur` | ✅ |
| 3 | `ProductLink` — رقم ربط مستقلّ لكل **(باقة × مزوّد)** + `extra` لما يزيد | ✅ |
| 4 | حلقة المراقبة `sync_pending` — تستعلم `pin_kontrol` كل 6s وتتوقّف وحدها | ✅ |
| 5 | إلغاء المزوّد (حالة 3) ⇒ يُجرَّب البديل، وإن لم يلتقطه أحد استُرجع المبلغ | ✅ |
| 6 | اعتماد **تكلفة المزوّد الفعلية** من ردّه بدل التكلفة المقدَّرة على المنتج | ✅ |
| 7 | **منع البيع بخسارة** (`loss_guard`) — يُتخطّى المزوّد قبل إنفاق المال، ويتعلّم السعر المرجعي من أول ردّ | ✅ |
| 8 | النجاح **بلا PIN** حالة صحيحة — «شُحن مباشرةً ✓» للباقات التي تُشحن على معرّف اللاعب | ✅ |
| 9 | لوحة الوكيل ترى **أرقامها هي**: شراؤه · سعر بيعه لزبونه (توصية أو يدوي) · ربحه — ولا تُكشَف له تكلفة المزوّد ولا ربح المالك | ✅ |
| 10 | كتالوج ZNET الحيّ (`pin_listesi` → 752 باقة) لصفحة ربط الباقات | ✅ |

### ما بقي مفتوحاً في مسار ZNET

- ~~لا بديل~~ ✅ **حُلّ**: زينت رئيسياً وأب ستور بديلاً — انظر §4.
- **`http://` لا `https://`**: `kod`/`sifre` يُرسلان في query string غير مشفّر.
- **حلقة المراقبة تعمل من لوحة الأدمن فقط**: طلب «قيد التنفيذ» عند الوكيل لا
  ينقلب إلى «ناجح» ما لم يكن الأدمن على لوحته أو يُحدَّث الجدول يدوياً.

---

## 2. التالي 🔜 — التوجيه إلى برمجية **ZDK**

الهدف: مزوّد ثانٍ يعمل بالتوازي مع ZNET، فيصير له في سلسلة التوجيه موضع
حقيقي (`API 1`) بدل «بديل مغلق» — وبه يُغلق أول بند من المفتوحات أعلاه.

### توحيد التسمية ✅ (2026-07-31)

`Barakat` و`Apstore` ليسا برمجيتين بل **متجران على برمجية ZDK** — API واحد
لهما. كان النوع عندنا باسم متجر (`barakat`)، فكل متجر جديد على البرمجية
نفسها كان سيضيف اسماً جديداً بلا داعٍ. صار النوع **`zdk`** باسم البرمجية:

- `providers/adapters/barakat.py` → **`zdk.py`** · `BarakatAdapter` → **`ZdkAdapter`**
- نافذة إضافة مزوّد: «Barakat / Apstore (خارجي)» → **«ZDK (خارجي)»**
- الكودان `barakat`/`apstore` ما زالا مقبولَين — المزوّدون المُعدّون سابقاً
  يعملون بلا لمس، والجديد يُحفظ بـ`zdk`.

### الوثيقة الرسمية ✅ (2026-07-31)

وصلت: <https://api.ap4stor.com/api-docs> — حُفظ ملخّصها المعتمد في
[`docs/integrations/ZDK_API.md`](docs/integrations/ZDK_API.md). وكشفت **ثلاثة
فروق** عن المواصفة المستخرَجة التي بُني عليها المحوّل، صُحّحت كلّها:

| # | كان | صار |
|---|-----|-----|
| 1 | معرّف اللاعب يُرسل في `extra` | `playerId` — الاسم الذي توثّقه ZDK |
| 2 | `order_uuid` = رقم الفيش (ليس UUID) | `uuid5` مشتقّ من رقم الفيش — صيغة صحيحة **وثابتة**، والوثيقة تنصّ أن الطلب idempotent بالـ uuid فإعادة المحاولة لا تشحن اللاعب مرّتين |
| 3 | **لا `fetch_status` إطلاقاً** — حلقة المراقبة عمياء عن ZDK | `GET /client/api/check?orders=…[&uuid=1]` |

وأُضيف: قراءة `replay_api` (رسالة المزوّد) كملاحظة · عرض `params` المطلوبة لكل
منتج في الكتالوج · أكواد الأخطاء (120 توكن · 100 رصيد) تُعرض كما هي.

### الربط الحيّ ✅ (2026-07-31)

| # | الخطوة | النتيجة |
|---|--------|---------|
| 1 | إضافة المزوّد `ap4stor` بالتوكن وحده | ✅ |
| 2 | `refresh-balance` → `client/api/profile` | ✅ الرصيد يُجلب |
| 3 | **طلب فعلي** — الفيش `26073178940` · باقة 60 UC | ✅ **استقبله ZDK** وأعاد تكلفته الفعلية **43.18** فاعتمدها النظام تلقائياً (ربح 1.82 على بيع 45.00) |
| 4 | **رفض المزوّد** للطلب نفسه (ردّه: «مرفوض») | ⚠️ الرفض وصل، لكن **لا استرجاع تلقائي** — انظر التصحيح أدناه |

اعتماد التكلفة الفعلية من ردّ ZDK دليل قاطع أن المسار كامل: الإرسال وصل،
والردّ فُهم، و`_apply_real_cost` عمل على مزوّد لم يُختبر قبلاً.

**ملاحظة على التشخيص:** أول محاولة ردّت
`تعذّر الاتصال بـ ZDK: Expecting value: line 1 column 1` — والرسالة **مضلِّلة**:
`requests.exceptions.JSONDecodeError` يرث من `RequestException`، فخطأ في
تحليل ردّ **وصل** يُعرض كأنه فشل اتصال. النقطة مفتوحة في §5 أدناه.

**تصحيح — دورة الرفض ليست مؤكَّدة:** وُثِّق أوّلاً أن الرفض مرّ عبر
`fetch_status` مع استرجاع تلقائي. **هذا خطأ:** تبيّن أن الموقع الحيّ كان يشغّل
كوداً أقدم من إضافة `fetch_status` (تفاصيل السبب في §5 البند 6). فما جرى
فعلاً أن ZDK ردّ بالرفض **لحظة الإرسال**، فاعتبره المحوّل القديم فشلاً وسقطت
سلسلة التوجيه ⇒ الطلب **«عالق» بلا استرجاع**.

يترتّب على ذلك أمران:
1. مبلغ ذلك الطلب قد يكون ما زال مخصوماً من محفظة الوكيل — يُتحقَّق منه.
2. سؤال «هل يحسم ZDK فوراً أم يردّ `wait`؟» **ما زال مفتوحاً**، ولا يُحسم إلا
   بتجربة على الكود المنشور فعلاً.

**غير مؤكَّد حيّاً بعد:** دورة النجاح (`accept`) · دورة الرفض عبر `check` ·
هل يحمل `replay_api` كود الشحن؟ · هل يقبل `check` الاستعلام بالـ uuid؟

صحّ التوقّع: العمل انحصر في **محوّل واحد** + تسجيله + وثيقة. البنية العامة
(`adapter_for` · `ProductLink` · حماية الخسارة · حلقة المراقبة · الاسترجاع)
استقبلت المزوّد الجديد بلا تعديل.

---

## 3. تحكّم المشغّل بالطلب ✅ (2026-07-31)

الطلب لم يعد رهين ما أُعدّ عليه وقت إنشائه. يحدّد المشغّل طلباً أو أكثر في
«متابعة الطلبات» فيظهر شريط إجراءات فوق الجدول:

| الإجراء | الأثر | يعمل على |
|---------|-------|----------|
| **قبول** | ناجح — بلا اختلاق PIN وهمي | قيد الانتظار · قيد التنفيذ · عالق |
| **رفض** | ملغى + استرجاع المبلغ للوكيل | كل ما لم يُحسم |
| **توجيه للمزوّد** | إرسال إلى مزوّد يختاره من القائمة، متخطّياً سلسلة المنتج | قيد الانتظار · عالق |
| **إعادة إلى يدوي** | يعود «قيد الانتظار» ويُفكّ عن مزوّده | كل ما لم يُحسم |
| **ملاحظة** | تُحفظ في `dealer_note` — **يراها الوكيل** في تفاصيل طلبه | مع أي إجراء |

يغلق هذا حالتين كانتا مسدودتين: باقة أُعدّت «يدوي» ثم قرّر المشغّل توجيهها،
وطلب **عالق** لدى مزوّد يُعاد إلى اليدوي ثم يُوجَّه إلى مزوّد آخر أو يُنفَّذ بيده.

**بُني على الطريق:**
- `execute_order` كان **يختلق PIN وهمياً** (`PIN-123456`) عند التنفيذ اليدوي
  بلا كود — يُسلَّم للوكيل كأنه كود شحن حقيقي. صار يترك الحقل فارغاً فتُعرض
  «شُحن مباشرةً ✓».
- جسم `dispatch_order` استُخرج إلى `_send_to()` يتشاركه التوجيه التلقائي
  واليدوي — فحماية الخسارة وتعلّم التكلفة تعملان في المسارين بلا ازدواج.
- قفل صفّ الطلب (`select_for_update`) في القبول/الرفض/الإعادة — يمنع
  استرجاعين للمبلغ نفسه عند تنفيذ إجراءين متزامنين.

---

## 4. سياسة السلسلة البديلة (Failover) ✅ (2026-07-31)

توجيه الباقة ثلاثة مواضع: **الرئيسي** ثم **API 1** ثم **API 2**. والانتقال
بينها يقع في حالتين لا واحدة:

| متى | ما يحدث |
|-----|---------|
| **المزوّد يرفض لحظة الإرسال** | يُجرَّب التالي فوراً في النداء نفسه (`dispatch_order`) |
| **المزوّد يقبل ثم يلغي لاحقاً** | تلتقطه حلقة المراقبة فتُجرِّب من بعده (`_failover_after_rejection`) |

### تعمل مع ثلاثة مزوّدين كما مع اثنين

القاعدة تُعاد عند **كل** إلغاء لا مرّة واحدة، فالسلسلة تنزل درجةً درجة:

```
الرئيسي (زينت) يقبل → يلغي → API 1 (أب ستور) يقبل → يلغي → API 2 يُجرَّب
                                                              │
                                        لم يلتقطه أحد ⇒ ملغى + استرجاع
```

كل انتقال يبدأ من **موضع مَن ألغى + 1**، فلا يُعاد الإرسال إلى من جُرِّب —
وهو ما قد يشحن اللاعب مرّتين ويدفع التكلفة مرّتين.

### قواعد مقرّرة من المالك

1. **لا إعادة محاولة على من ألغى.** ننتقل إلى التالي مباشرةً.
2. **الطلب المُوجَّه يدوياً يُكمل السلسلة آلياً** كأي طلب. وإن كان المزوّد
   الذي اختاره المشغّل خارج سلسلة الباقة، تُجرَّب السلسلة كاملةً ما عدا
   مَن ألغى للتوّ.
3. **تحويل الطلب إلى «يدوي» يقطع كل شيء**: يُفكّ عن مزوّده وتسقط متابعته،
   لأن حلقة المراقبة لا تسأل إلا عمّا حالته «قيد التنفيذ».

### مؤكَّد حيّاً

زينت رئيسياً وأب ستور بديلاً · أُرسل الطلب فقبِله زينت ثم أُبطل من لوحته
⇒ **انتقل إلى أب ستور وحده** وبقي الطلب حيّاً بلا استرجاع. السلسلة تعمل
عبر برمجيتين مختلفتين (ZNET نصّي · ZDK بـJSON) بلا كود خاصّ بأيّهما.

### ما يُفسد الانتقال بلا أن يكون خللاً فيه

- **لا رقم ربط** للباقة لدى البديل ⇒ يرفض فوراً.
- **حماية الخسارة**: تكلفة البديل أعلى من سعر بيعنا ⇒ يُتخطّى ويُكتب السبب.
- **اللوحة مغلقة**: لا عامل خلفي، ومتصفّح المشغّل هو من يسأل المزوّد —
  فالإلغاء لا يُكتشف أصلاً ولا يقع انتقال (البند 1 من §5 — لا عامل خلفي).

سجلّ المحاولات كاملاً في «تفاصيل المسار» (زرّ 🔍): من ألغى، ومن التقط،
ولماذا تُخطّي الباقون.

---

## 5. ديون تقنية مفتوحة

| # | البند | الأثر | الحل المقترح |
|---|-------|-------|---------------|
| 1 | **لا عامل خلفي** — المتابعة يحرّكها متصفّح الأدمن (`sync-pending` كل 6s) | لا أحد يفتح اللوحة ⇒ الطلبات «قيد التنفيذ» تبقى معلّقة للأبد. وعند التوسّع: نبضة واحدة قد تتجاوز مهلة الخادم وتشغل نصف عمّاله | أمر مجدول (Render Cron) كل دقيقة + قفل على الطلب. Celery لاحقاً عند الحجم |
| 2 | **رسالة خطأ مضلِّلة** — `requests.exceptions.JSONDecodeError` يرث من `RequestException` فيُعرض فشل التحليل كأنه فشل اتصال | ضاع وقت في تشخيص خطأ ZDK الأول | فصل التقاط الاستثناءين وإظهار رمز HTTP وأوّل الجسم |
| 3 | حلقة المراقبة لا تعمل من لوحة الوكيل | طلب الوكيل «قيد التنفيذ» لا ينقلب إلا بتحديث الصفحة | يحلّه البند 1 |
| 4 | ~~لا سلسلة بديلة~~ ✅ **مفعّلة** | ZNET رئيسياً وZDK بديلاً — والانتقال يعمل في الحالتين: فشل الإرسال، **وإلغاء المزوّد بعد قبوله** | — |
| 5 | ZNET على `http://` غير مشفّر | `kod`/`sifre` مكشوفان في الرابط | تحويل إلى `https` إن دعمه المزوّد |
| 6 | ~~فحص الواجهة كان أعمى~~ ✅ **حُلّ** | `npx tsc --noEmit -p tsconfig.json` كان يمرّ نظيفاً **بلا فحص شيء**: الجذر ملفّ حلول (`references`) لا يفحص وحده. فمرّ خطأ نوع إلى المستودع، و**فشل بناء الإنتاج خمسة التزامات متتالية** فبقي الموقع على كود قديم بينما نظنّه محدَّثاً | الفحص المعتمد صار `npm run build` (`tsc -b && vite build`) — هو أمر الـ Dockerfile نفسه |

---

# جلسة 2026-08-01 — المال والعملات وبنية الأقسام

أحد عشر التزاماً من `d068008` إلى `a646924`، كلّها منشورة على
<https://wtn.onrender.com>.

---

## 6. قسم «إضافة رصيد» لدى الوكيل ✅

الوكيل كان يقرأ في محفظته: *«لشحن رصيدك تواصل مع صاحب المتجر»*. صار له تبويب
**«إضافة رصيد»** على نمط لوحات ZDK التي أرسل المالك صورها.

### ما يراه الوكيل

شبكة بطاقات لطرق الدفع (صورة أو لون + اسم + سطر فرعي). عند فتح بطاقة تظهر
صفحة الطريقة كما ضبطها المالك: الشرح سطراً سطراً · صندوق الحساب الداكن
**يُنسخ بضغطة** · الحقول التي بناها المالك · «القيمة» بعملة الطريقة ·
**«القيمة التي سيتم إضافتها»** محسوبةً لحظياً · زرّ «طلب». وتحتها سجلّ
طلباته ملوّناً بحالاتها مع ملاحظة الإدارة.

### ما يتحكّم به المالك — «الوكلاء ← طرق الدفع»

الاسم · السطر الفرعي · صورة البطاقة أو لونها · الترتيب · العملة · الحدّان
الأدنى والأعلى · العمولة % · حساب الاستلام المرتبط · نصّ الشرح · صندوق
الحساب · التنبيه · التفعيل والتعطيل.

**باني حقول حرّ** لكل طريقة: نص · رقم · نص طويل · قائمة اختيار · رابط صورة،
مع الإلزام والترتيب. **الخادم** هو من يتحقّق من الإلزامي لا الواجهة.

### قرار المالك في الموافقة

**لا قبول تلقائي إطلاقاً.** كل طلب ينتظر قراره في «متابعة الدفع»، والجدول
أُعيد بناؤه على تصميم جدول طلبات الألعاب: نقاط فلترة بالحالة · لوحة فلاتر ·
تحديد الصفوف · شريط إجراءات بملاحظة تصل الوكيل. **وعكس القرار** كما هناك:
إبطال المقبول يسحب المبلغ، وقبول المرفوض يضيفه — وكل حركة في دفتر الأستاذ
مع الرصيد قبل وبعد.

### بنية البيانات

`PaymentMethod` + `PaymentMethodField` جديدان، و`PaymentNotification` القائم
وُسِّع بـ(الطريقة · العملة · سعر الصرف · العمولة · المبلغ المُضاف · قيم
الحقول · ملاحظة المالك · وقت القرار · الرصيد قبل/بعد). **لم يُنشأ نظام
إيداع ثانٍ** — الموجود وُسِّع، فبقي مسار موافقة واحد.

بذور `seed_payment_methods` متسامحة تعمل كل نشر: أربع طرق جاهزة (تواصل
دولار · شام كاش · حوالات مكتب · تسليم باليد) **ولا تدهس تعديلات المالك**.

---

## 7. العملات — دفتر بالدولار وعملة عرض لكل وكيل ✅

### القرار ولماذا

سأل المالك: دولار حصراً أم عملات متعدّدة؟ الجواب المتّفق عليه يفصل أمرين:

- **عملة الدفتر واحدة = الدولار.** كل رقم في قاعدة البيانات محفوظ بها.
- **عملة العرض تخصّ كل وكيل.** تحويلٌ عند الخروج والدخول فقط.

**سبب الفصل:** لو حُفظت الأرصدة بعملتين لصار عكسُ أي قرار رهينةَ سعر صرف يوم
آخر — تُلغي طلباً خُصم الأحد فتردّه الأربعاء بسعر مختلف، فيخسر أحد الطرفين
بلا سبب. وهذا يقع يومياً لا نادراً.

**ولماذا الدولار:** تكاليف المزوّدين بالدولار غالباً، والليرتان متقلّبتان —
دفترٌ بهما يعني مطاردة أسعار كل الباقات يدوياً مع كل حركة سوق.

### الآلية — `backend/core/currency.py`

| الدالة | متى تُستعمل |
|--------|-------------|
| `to_display` | كل رقم يخرج إلى الوكيل |
| `from_display` | كل رقم **يكتبه** الوكيل (سعر بيعه لزبونه) |
| `display_currency` | عملة بلا سعر صرف مضبوط ترجع تلقائياً لعملة الموقع بدل رقم خاطئ |

**مُحوَّل:** كتالوج الوكيل · الشراء · طلباته · محفظته · ملخّصه · تقريره ·
طرق الدفع وطلبات الإيداع · **ولوحة الوكيل الكبير كاملةً** (رصيده وأرباحه ·
أرصدة دكاكينه · طلباتهم · تكلفته وسعر دكاكينه في هامش الأسعار — والهامش
نسبة مئوية فلا يُحوَّل).

**غير مُحوَّل عمداً:** لوحة صاحب المتجر ولوحة مالك المنصّة — بعملة الدفتر
دائماً.

### اصطلاح سعر الصرف — قُلب بطلب المالك

`exchange_rates[CUR]` = **كم وحدةً من CUR تساوي وحدةً من عملة الموقع**.
دفترٌ بالدولار ⇒ `{"TRY": "41.5"}` تعني «1$ = 41.5 ل.ت».

كان الاصطلاح مقلوباً (الحقل يطلب «1 ليرة = كم دولاراً؟» فيُكتب `0.024096`).
قُلبت **الدلالة المخزَّنة** لا العرض وحده: لو قُلب العرض فقط لكان المالك
يكتب 41.5 فيُحفظ مقلوبه، ثم يُعاد حسابه فيظهر 41.5004 — رقمٌ لم يكتبه.

### أسعار الصرف — قسم مستقلّ

**الإعدادات ← أسعار الصرف**: عملة الموقع (مع تحذير عند تغييرها لأن الأرقام
المحفوظة لا تُحوَّل تلقائياً) · جدول العملات بالسعر والاتجاه المعاكس المحسوب
وحالة الضبط · محوّل تحقّق سريع. **مرجع واحد** تقرأ منه كل الأقسام.

> ⚠ **قاعدة:** عملة بلا سعر صرف لا تُقبل لعرض وكيل، ولا لطريقة دفع. الخادم
> يرفضها بدل أن يحسب بمعامل 1 صامت — وهو خطأ كان يجعل 100 يورو تدخل الدفتر
> 100 دولار.

### التحويل الفعلي للدفتر

| الأداة | متى |
|--------|-----|
| `core.0015_ledger_to_usd` | ترحيل بيانات نفّذ التحويل مرّةً واحدة عند النشر (بسعر الدولار المحفوظ، أو 41.5) |
| `core.0016_flip_exchange_rate_direction` | قلب اتجاه الأسعار المحفوظة — لا يمسّ أي مبلغ، ومعكوسٌ لنفسه |
| `manage.py convert_base_currency` | أمر يدوي لأي تحويل لاحق — معاينة ثم `--apply` داخل معاملة واحدة |

**لم يُمسّ:** اشتراك المنصّة وفواتيره (دفتر بين مالك المنصّة والمتجر) ·
مبالغ الإيداع بعملة طريقة الدفع.

> 🔎 **يبقى على المالك:** أسعار الصرف المزروعة **تقديرية** (41.5 للدولار).
> تُراجَع بأرقام السوق قبل إدخال بيانات حقيقية، وتُراجَع أسعار الباقات
> المحوَّلة لتصير أرقاماً تجارية مستريحة.

---

## 8. قائمة الوكلاء — نافذة إعدادات وتصدير وكشف حساب ✅

زرّ ⚙ في كل صفّ يفتح نافذة بتبويبين:

| التبويب | ما فيه |
|---------|--------|
| **الحساب** | عملة الوكيل · الحد الائتماني · الحالة (نشط/موقوف/قائمة سوداء) · تغيير كلمة السر بتأكيد |
| **بيانات الوكيل** | رقم الجوال · البلد والمحافظة · صورة الهوية · صورة لوحة المحل · موافقة تحصيل الدَّين آلياً |

وأُضيف عمود **«العملة»** إلى الجدول برمزها وحده، وأُزيل عمود «الدولة» (صار
داخل النافذة).

**قراران وسببهما:**

1. **الصور تُحفظ `data URL` في القاعدة** لا ملفّاتٍ على القرص — قرص الاستضافة
   مؤقّت يُمسح مع كل نشر، فالوثائق كانت ستضيع عند أول تحديث. وتُصغَّر في
   المتصفّح إلى 1400px/JPEG قبل الإرسال (هوية 4MB ← ~150KB)، والخادم يرفض ما
   ليس صورةً وما تجاوز الحدّ.
2. **الحد الائتماني يُرفض موجباً** — هو أقصى دَين مسموح؛ `500` كان سيمنح
   الوكيل مالاً لم يدفعه.

**الزرّان المعطّلان أُنجزا:**

- **تصدير Excel** — يصدّر الظاهرين بعد البحث والفلترة لا القائمة كاملةً.
  CSV بفاصلة منقوطة و**BOM**: بدونهما يعرض Excel العربية مشوّهة ويقسم الأرقام
  العشرية على أعمدة في ويندوز العربي.
- **كشف الحساب** — نافذة بحركات المحفظة، إجماليات الوارد والصادر، فلترة
  وارد/صادر، تصدير وطباعة. تعرض آخر 100 حركة (حدّ الـ API القائم).

**موافقة تحصيل الدَّين** تسجيل موافقة لا غير — لا يجري أي تحصيل. عليها يُبنى
روبوت التحصيل لاحقاً (انظر §10).

---

## 9. بنية الأقسام ✅

### الشريط العلوي صار خمسة

| القسم | أقسامه الفرعية |
|-------|----------------|
| الرئيسية | — |
| الألعاب | كما كانت |
| **الوكلاء** | قائمة الوكلاء · متابعة الدفع · طرق الدفع · أسعار الصرف · حساباتي · حركات الحسابات |
| **الإعدادات** | إعدادات الموقع · SMS · الرسائل · فواتير الاشتراك |
| التقارير | كما كانت |

مسارات قسم الإعدادات نُقلت إلى `/settings/*` ليبقى تحديد التبويب النشط
قاطعاً — لو تشارك القسمان بادئة `/ayarlar` لعاد الالتباس.

### ما حُذف

- **«مجموعات الوكلاء»** — كاملةً: الصفحة والمسار والـ API والنموذج وجدول
  `dealer_groups` (ترحيل `0018`). كانت معزولة تماماً؛ حقل «المجموعة» في بطاقة
  الوكيل نصٌّ حرّ في `modules` ولا علاقة له بها فبقي.
- **لون الثيم وخط الموقع** من إعدادات الموقع — مكانهما لوحة «المظهر».
  الحقول باقية في النموذج لأن لوحة مالك المنصّة تضبطها لكل متجر.

### إصلاحات أصغر

- الشريط العلوي كان يُبرز «الرئيسية» دائماً — صار تبويب واحد نشط.
- «متابعة الدفع» تفتح على «الكل» بدل «قيد المراجعة».
- **مجموعات الأسعار**: عمودا «التكلفة» و«الموصى» صارا قابلَين للتعديل بالنقر،
  ويحفظان في نفس حقلَي المنتج اللذين تحرّرهما باقات المنتجات — التعديل من
  أيّ الجهتين يظهر في الأخرى.

---

## 10. ما تبقّى — نقطة البداية غداً

### أ. متعلّق بما بُني اليوم

| # | البند | لماذا |
|---|-------|-------|
| 1 | **مراجعة أسعار الصرف والباقات** | المزروع تقديري لا سوقي — انظر §7 |
| 2 | **«إدخال كود الشحن»** (كوبونات رصيد) | أجّله المالك عمداً؛ يظهر في لوحات ZDK أعلى صفحة إضافة الرصيد |
| 3 | **روبوت تحصيل الدَّين** | الموافقة تُسجَّل الآن (§8) ولا شيء يعمل عليها بعد |
| 4 | **تنبيه الوكلاء** بأن رصيدهم المعروض بعملة أخرى يتغيّر رقمه مع سعر الصرف بلا حركة منهم | كُتب للمالك في اللوحة، ولم يصل الوكلاء |
| 5 | **إشعار الوكيل** عند قبول/رفض إيداعه | يراه في سجلّه فقط إن فتحه |
| 6 | «قائمة الوكلاء» لا تعرض الوكيل الكبير | سلوك قائم من قبل — يُحسم إن أراده المالك |
| 7 | «تصدير Excel» في بقيّة الجداول | صار جاهزاً في `frontend/src/csv.ts` — تركيبه في أي جدول سطران |
| 8 | **نقل فحوص الدخان إلى المستودع** | 146 حالة تعمل يدوياً الآن — انظر §11 |

### ب. ديون قديمة ما زالت مفتوحة

**البند 1 من §5 هو الأهمّ:** لا عامل خلفي. متصفّح الأدمن هو من يحرّك متابعة
الطلبات — تُغلق اللوحة فتتجمّد الطلبات «قيد التنفيذ» للأبد. حلّه أمرٌ مجدول
(Render Cron) كل دقيقة.

وبقيّة الجدول في §5 على حالها: رسالة خطأ ZDK المضلِّلة · ZNET على `http` ·
دورات ZDK غير المؤكَّدة حيّاً (§2).

---

## 11. أدوات الفحص المعتمدة

| الفحص | الأمر |
|-------|-------|
| الواجهة | `npm run build` — **وحده** يفحص فعلاً (§5 البند 6) |
| الخلفية | `manage.py check` |
| الترحيلات | `migrate` على SQLite من قاعدة فارغة، ثم البذور بعده |

فحوص الدخان لهذه الجلسة (146 حالة) في مجلّد الجلسة المؤقّت لا في المستودع:
مسار الإيداع · عملة العرض · لوحة الوكيل الكبير · ترحيل تحويل الدفتر ·
نافذة إعدادات الوكيل. **يستحقّ نقلها إلى `backend/*/tests/` لتعمل دائماً** —
البند 8 أعلاه.

> **ملاحظة على الفحص المحلّي:** Postgres المحلّي غير مشغَّل، فأوامر Django
> تتجمّد على مهلة الاتصال. الحيلة: `DB_HOST=127.0.0.1 DB_PORT=59999` تجعلها
> تفشل فوراً، و`makemigrations`/`check` يعملان بلا قاعدة. والفحوص تُشغَّل على
> SQLite عبر إعدادات بديلة تستورد `config.settings` وتُبدّل `DATABASES`.

---

# جلسة 2026-08-02 — واتساب

الفكرة من المالك: لكل صاحب متجر وكلاء، ويريد مراسلتهم عبر واتساب — رسالة
تنبيه بالرصيد لوكيل بعينه، ورسالة جماعية لكل من عليه دَين. المرجع الذي قدّمه
تقرير مشروع «مطابقة» ([whatsappReport.md](whatsappReport.md)).

---

## 12. لماذا لم نَنسخ التقرير كما هو

التقرير سليم معمارياً، لكنه كُتب لمشروع يعيش في `docker-compose` على خادم
دائم. عندنا **ثلاثة من افتراضاته تنهار**:

| افتراض التقرير | واقعنا | ما كان سيحدث |
|---|---|---|
| قرص دائم لمجلّد `auth_sessions/` | قرص Render المجاني **يُمسح مع كل نشر** | مسح رمز QR من جديد بعد **كل** `git push` |
| عملية Node دائمة الاتصال | الخطة المجانية **تنام بعد ~15 دقيقة** | قطع واتصال عشرات المرّات يومياً — أوضح بصمة آلية يعاقب عليها واتساب بالحظر |
| طابور خلفي (Celery/BullMQ) | **لا عامل خلفي عندنا أصلاً** (§5 البند 1) | لا مكان يعمل فيه الإرسال البطيء المتقطّع |

الافتراض الأول ليس درساً جديداً: في §8 حفظنا صور الهوية `data URL` في القاعدة
للسبب نفسه. ومفاتيح جلسة واتساب أخطر من صورة هوية، فسلكت الطريق نفسه.

---

## 13. القرار المعماري — البوت يسحب ولا يُدفع إليه ✅

التقرير يجعل **Django ينادي البوت**. عكسناها:

```
Django  →  يكتب صفّاً في طابور OutboundMessage  ←  البوت يسأل كل 3 ثوانٍ
                                                     يرسل · يعيد النتيجة
```

**ما اشتراه القلب — أربعة أشياء دفعةً واحدة:**

1. **البوت هو العامل الخلفي.** الطابور جدولٌ في القاعدة، والتهدئة حلقةٌ في
   البوت. لا Celery ولا Redis ولا طابور ثالث.
2. **لا يحتاج البوت عنواناً عاماً ولا منفذاً مفتوحاً** — فيعمل من جهاز المالك
   خلف أي راوتر، وينتقل إلى سيرفر مدفوع بتغيير `SITE_URL` وحده.
3. **الإرسال ينجو من نوم Render**: الصفوف تنتظر في القاعدة، والبوت — وهو
   المستيقظ — يستهلكها. وما دام يسأل، **لا ينام الموقع** أصلاً.
4. **مفاتيح الجلسة في القاعدة لا على القرص** ⇒ تُنسخ نسخة البوت إلى سيرفر
   بنفس المفتاح فتُستأنف الجلسة نفسها — **رمز QR يُمسح مرّةً واحدة في عمر
   المشروع**.

**والتشفير يقع على جهاز البوت لا في الموقع:** `BOT_ENCRYPTION_KEY` لا يغادر
الجهاز (AES-256-GCM + مفتاح فرعي بـHKDF لكل مفتاح). فالقاعدة تخزّن نصّاً
معمّى لا تفهمه — ولو سُرّبت نسخة احتياطية بقيت الجلسات عديمة النفع.

---

## 14. قرارات المالك في هذه الجلسة

| السؤال | القرار |
|--------|--------|
| عملة الرصيد في الرسالة | **بعملة عرض الوكيل** — «دَينك 20,500 ليرة تركية» |
| مَن يكتب نصّ الرسالة | صاحب المتجر، من **الإعدادات ← واتساب** |
| مَن يملك رقم واتساب | صاحب النسخة (`tenant_admin`) وحده — جلسة لكل متجر. الوكيل الكبير لا جلسة له |
| الأزرار | زرّ جماعي أعلى جدول الوكلاء + زرّ فردي في كل صفّ |
| نطاق الجماعي | **رصيد سالب + موقّع على «موافقة تحصيل الدَّين آلياً»** |
| المرحلة | **الإرسال أولاً** — الاستقبال من مجموعات الجرد لاحقاً |
| الاستضافة | المجاني الآن (البوت على جهاز المالك)، وسيرفر مدفوع لاحقاً |

**القسم الفرعي:** طلب المالك «الإعدادات ← الرسائل»، ووُضع في تبويب جديد اسمه
**«واتساب»** بجانبه — لأن «الرسائل» هي صندوق التذاكر مع الوكلاء، فلو سكن
القالب فيها لاختلط صندوق وارد بإعدادات إرسال. نقله سطرٌ واحد إن أراده هناك.

---

## 15. ما بُني ✅

### الخلفية — تطبيق `whatsapp` جديد

| النموذج | الوظيفة |
|---------|---------|
| `WhatsAppAccount` | صفّ لكل متجر: حالة الجلسة · الأمر المعلّق (`want`) · رمز QR · الرقم المرتبط · القالب · التهدئة |
| `WhatsAppAuthKey` | مفاتيح Baileys — مفتاحٌ لكل «ملف»، قيمته معمّاة |
| `OutboundMessage` | الطابور: لمن · النصّ · الحالة · المحاولات · سبب الفشل · معرّف الدفعة |

وحقل `User.whatsapp` — **مطبَّعاً عند الحفظ لا عند الإرسال**
([whatsapp/phone.py](backend/whatsapp/phone.py)): يقبل `+90…` و`0090…`
و`05…` والأرقام العربية، ويردّ ما لا يصلح برسالة بدل أن يفشل صامتاً وقت
الإرسال. وفيه فخّ عالجناه: «0963944…» — صفرٌ ثم رمز دولة — كان يخرج برمز
مضاعف يُرسَل إلى لا أحد.

**بناء الرسالة** ([whatsapp/messages.py](backend/whatsapp/messages.py)) يمرّ
بـ`to_display` دائماً. القاعدة: لو أرسلنا الرقم كما هو في الدفتر (‎-500$)
لقرأ وكيلٌ تركي في واتساب رقماً لا يطابق ما يراه في لوحته (‎-20,750 ل.ت) —
**رقمان لدَينٍ واحد في رسالة تطالبه بالدفع**.

العناصر النائبة: `{الاسم}` `{الرقم}` `{الرصيد}` `{الدين}` `{العملة}`
`{الرمز}` `{المتجر}` — ولكلٍّ مرادف إنجليزي.

### الواجهة

- **الإعدادات ← واتساب**: حالة الربط وعرض QR (باستفتاء كل 2.5s) · محرّر
  القالب بعناصر تُدرَج **عند مؤشّر الكتابة** · **معاينة حيّة** على وكيل حقيقي
  في فقاعة واتساب · ضبط التهدئة · جدول الطابور بمن أُرسل ومن فشل ولماذا ·
  زرّ «إلغاء المعلّق».
- **قائمة الوكلاء**: زرّ واتساب في كل صفّ (رماديّ ومعطّل بلا رقم)، وزرّ
  **«تنبيه المدينين»** أعلى الجدول.
- **نافذة الإرسال الجماعي** لا ترسل قبل أن تقول الحقيقة: ثلاثة عدّادات قابلة
  للنقر — **سيصلهم** · **لم يوقّعوا الموافقة** · **بلا رقم** — وتحت كلٍّ
  قائمته بالأسماء والأرصدة **بعملة كل وكيل**.
- **إعدادات الوكيل ← بيانات الوكيل**: حقل «رقم واتساب (برمز الدولة)».

### البوت — مجلّد [`bot/`](bot/)

Node + Baileys 6.7، بلا واجهة ولا منفذ. يسأل، ويوزّع الأوامر، ويرسل بتهدئة.
تفاصيل التشغيل في [bot/README.md](bot/README.md).

**احتياطات الحظر المطبَّقة:** تأخير عشوائي 4–10 ثوانٍ بين كل رسالتين ·
استراحة بعد كل دفعة (25 ← 5 دقائق) · التحقّق أن الرقم مسجَّل على واتساب
(`onWhatsApp`) قبل الإرسال · نصٌّ يحمل اسم كل وكيل ورصيده فلا رسائل متطابقة ·
`markOnlineOnConnect: false` و`syncFullHistory: false`.

---

## 16. تفاصيل صغيرة لها سبب

- **الواجهة الداخلية مغلقة (503) ما لم يُضبط `INTERNAL_API_KEY`** — لا نترك
  باباً مفتوحاً بحجّة أن أحداً لا يعرف مساره. والمقارنة ثابتة الزمن.
- **الموقع لا يحجز رسالةً إلا لمتجر أعلن جهوزيته** (`ready` في النبضة) —
  وإلّا لتراكمت رسائل «جارٍ الإرسال» في ذاكرة البوت وعلقت لو مات فجأة.
- **الحجز بتبديل حالة ذرّي** لا بقفل صفّ: `filter(status=pending).update(...)`
  ومن نجح تبديله يملكها. يعمل على Postgres وSQLite، وآمن لو عمل بوتان بالغلط.
- **ما بقي «جارٍ الإرسال» أكثر من 5 دقائق يعود للطابور** — البوت مات وهو
  يرسل. وبعد ثلاث محاولات يُعلَن فاشلاً بدل أن يدور بلا نهاية.
- **رمز QR يُمسح لحظة الاتصال** — هو مفتاح دخول، فلا يبقى محفوظاً بعد أن
  أدّى غرضه.
- **الزرّ الفردي لا يشترط رصيداً سالباً ولا موافقة** — المالك يقصد هذا الوكيل
  بعينه. شرط الموافقة للجماعي وحده.
- **إطفاء البوت لا يضيّع شيئاً**: الضغط يكتب في الطابور، والتشغيل يستأنف.
  والصفحة تُنبّه أن البوت مطفأ بدل أن تصمت.

---

## 17. ما تبقّى — نقطة البداية غداً

### أ. واتساب

| # | البند | لماذا |
|---|-------|-------|
| 1 | **تجربة حيّة**: رقم ثانوي · مسح QR · رسالة فردية ثم جماعية صغيرة | لم يُختبر مع واتساب الحقيقي بعد — الفحوص كلّها بلا شبكة |
| 2 | **الاستقبال** من مجموعات الجرد/الحسابات | المرحلة الثانية التي أجّلها المالك — تحتاج تحديد ما يُقرأ من المجموعة وكيف يُفهم ويُحفظ |
| 3 | **إشعار الوكيل بأنه سيُراسَل** عند توقيع موافقة التحصيل | نسجّل الموافقة، ولا نُعلمه أن رسائل ستصله |
| 4 | **سجلّ من أرسل ومتى** خارج آخر 100 رسالة | الطابور يعرض المئة الأخيرة فقط |
| 5 | نقل نبضة `sync_pending` إلى البوت | يغلق أقدم دَين عندنا (§5 البند 1) بلا خدمة جديدة |
| 6 | **تسخين الرقم** قبل أوّل إرسال جماعي حقيقي | على المالك لا على الكود — أهمّ حاجز عملي ضد الحظر |

### ب. ما زال من الجلسات السابقة

كل ما في §10 على حاله: مراجعة أسعار الصرف والباقات · كوبونات الرصيد ·
روبوت التحصيل (صار له أساس الآن) · إشعار قرار الإيداع · نقل فحوص الدخان إلى
المستودع. وديون §5: رسالة خطأ ZDK المضلِّلة · ZNET على `http` · دورات ZDK
غير المؤكَّدة حيّاً.

**فحوص هذه الجلسة:** 63 حالة (تطبيع الأرقام · بناء الرسالة بعملة العرض ·
فلترة الجماعي · عزل المتاجر · حماية الواجهة الداخلية · دورة الطابور كاملةً ·
مخزن المفاتيح) — تعمل على SQLite في مجلّد الجلسة المؤقّت، وتستحقّ النقل إلى
`backend/whatsapp/tests/` مع البند 8 من §10.

---

# جلسة 2026-08-02 (ب) — الجرد النهائي

«كشف الوكلاء» بُدِّل إلى **«الجرد النهائي»**. ولم يضِع بحذفه شيء: كان يقرأ
نفس بيانات «تقرير الأرباح» من نفس الـAPI، بعنوان آخر وعمود مُبرَز مختلف.
ومساره القديم `/reports/dealers` يُحوَّل إلى الجديد.

المرجع: نظام JRD الذي بناه المالك لجرد حساباته
([anotherproject/](anotherproject/)) + لقطات شاشته الثلاث.

---

## 18. ما اقتُبس وما لم يُقتبس

**الفكرة الأساسية مقتبسة كما هي:** الجرد لقطةٌ لكل مالك في لحظة، والفرق بين
لقطتين هو ربحك. وكذلك بنية الشاشة: جدول مصادر تلقائية · جدول بنود يدوية ·
ملخّص فيه مجاميع العملات وسعر الصرف والجرد السابق والربح الصافي.

**وما لم يُقتبس — ولا يصلح اقتباسه:**

| في JRD | عندنا | لماذا |
|--------|-------|-------|
| روبوتات Playwright وAPI لجلب كل رصيد | **قراءة من قاعدتنا** | أرصدة المزوّدين والوكلاء والحسابات عندنا أصلاً. الروبوت هناك كان لأن البيانات في مواقع الغير |
| عمودان ثابتان: ₺ و$ | **أي عملة مضبوط سعرها** | نظام العملات وأسعار الصرف مبنيّ عندنا (§7)، فتقييده بعملتين تراجُع |
| كل بند مالٌ لنا فيُجمع | **مبلغ موقّع: الموجب لنا والسالب علينا** | انظر §19 — هذا أهمّ فرق |

---

## 19. الفرق الجوهري — محافظ الوكلاء ليست مالنا ✅

نظام JRD لم يواجه هذا لأنه لا وكلاء فيه. وعندنا **وكيلٌ رصيده +500 دَينٌ
علينا**: قبضنا منه المال ولم نقدّم مقابله بعد.

لو جُمعت أرصدة الوكلاء ضمن رأس المال لصار كل شحن رصيد يقوم به وكيلٌ **ربحاً
على الورق** — والمالك يرى نفسه يربح كلّما ازداد ما عليه. فصار:

- كل سطر في الجرد **مبلغ موقّع**، والمجموع الكلي = صافي رأس المال مباشرةً.
- محافظ الوكلاء تدخل بسطر واحد = **سالب مجموعها**، وملاحظته تفصّل الوجهين:
  كم وكيلاً برصيد موجب (علينا) وكم برصيد سالب (لنا).
- الملخّص يعرض **الأصول** و**الالتزامات** مشتقَّين من إشارات الأسطر.

---

## 20. ما بُني ✅

### المصادر التلقائية — تُقرأ ولا تُجلَب

| المصدر | من أين | الإشارة |
|--------|--------|---------|
| **أرصدتنا لدى المزوّدين** | `Provider.real_balance` — سطر لكل مزوّد بالليرة | كما هي (السالب = مدينون له) |
| **حساباتنا والنقد** | `ReceivingAccount` المفعّلة | موجب |
| **محافظ الوكلاء** | مجموع `Wallet.balance` | **معكوس** |
| **اشتراك المنصّة** | الفواتير غير المدفوعة | سالب |

ولكل مصدر **مفتاح «يدخل الجرد»** — أي البنود تُحتسب قرارُ المالك لا الكود.

`Provider.balance` و`debt` اليدويان يُعرضان **ملاحظةً لا مبلغاً**: النموذج لا
يوثّق عملتهما، وجمع رقمٍ بعملة مجهولة يفسد الجرد كلّه.

### البنود اليدوية

جدول قابل للتحرير في مكانه: الاسم · المبلغ · العملة · الملاحظة. إضافةٌ
بالاسم وحده ثم يُملأ المبلغ. لما لا يعرفه النظام: نقدٌ في الصندوق، دَينٌ
شخصي، حوالة عند أحدهم، مصروف.

### الأزرار الثلاثة

| الزرّ | ما يفعل |
|------|---------|
| **جلب الأرصدة** | يسأل كل مزوّد عن رصيده الآن. **يُكمل على من فشل** ويسمّيه — مزوّدٌ معطوب لا يمنعك من جرد البقيّة. وفي صفّ كل مزوّد زرّ جلبٍ خاصّ به |
| **حفظ الجرد اليومي** | لقطة كاملة لا تتغيّر بعدها، وربحها = مجموعها − مجموع سابقتها |
| **جرد شهري** | لقطة تُقارَن بالشهري السابق، **و**تجمع أرباح اليوميات داخل الفترة. عرض الرقمين معاً مقصود: تباعدهما يعني جرداً يومياً ضائعاً |

### شاهدٌ مستقلّ لم يكن في JRD

كل لقطة تحفظ معها **ربح الطلبات في الفترة نفسها** (`Order.profit`). الجرد يرى
المال كلّه (نقداً وديوناً وأرصدة)، ودفتر الطلبات يرى المبيعات وحدها — فليسا
متطابقَين بالضرورة. لكن **تباعدهما الكبير سؤالٌ يستحقّ أن يُطرح**: تسرّب، أو
مصروف لم يُسجَّل، أو خطأ في بند.

### تفاصيل لها سبب

- **أسعار الصرف تُحفظ داخل اللقطة**: سعر اليوم ليس سعر الغد، ولو أُعيد الحساب
  لاحقاً بسعرٍ آخر لتغيّر ربحٌ سبق أن أُقفل.
- **دالّة بناء واحدة** ([services.py](backend/inventory/services.py)) تخدم العرض
  والحفظ — وإلّا لصار المحفوظ لا يطابق ما رآه المالك حين ضغط «حفظ».
- **كل مبلغ يخرج بخانتين عشريتين**: «0» و«0.00» في الردّ نفسه تربك الواجهة.
- **عملة بلا سعر صرف لا تُقبل لبند يدوي**، والبند القائم بعملة فقدت سعرها
  يُعلَّم «بلا سعر صرف» ويخرج من المجموع بدل أن يُحسب بمعامل 1 صامت.

**47 فحص دخان** نجحت: الإشارات · التحويل · البنود اليدوية · تعطيل المصادر ·
اللقطات والربح · الشهري مقابل مجموع اليوميات · العزل بين المتاجر · صلاحية
الوكيل · غياب سعر الصرف.

---

## 21. ما تبقّى في الجرد

| # | البند | لماذا |
|---|-------|-------|
| 1 | **تصدير ومقارنة**: PDF/Excel للقطة، ومقارنة لقطتين بنداً بنداً | JRD يصدّر PDF؛ وعندنا `csv.ts` جاهز |
| 2 | **رسم بياني** لتطوّر رأس المال والربح | يحتاج قراراً: مكتبة رسم أم SVG يدوي |
| 3 | **تنبيه عند تباعد** ربح الجرد عن ربح الطلبات | الرقمان محفوظان الآن، ولا تنبيه عليهما |
| 4 | **جرد تلقائي مجدول** (لقطة يومية بلا ضغطة) | يحتاج العامل الخلفي — وبوت واتساب صار موجوداً وقادراً عليه (§13) |
| 5 | **حماية الحذف**: حذف لقطة وسطى يترك أرباح ما بعدها محسوبةً على مفقود | التحذير مكتوب في الواجهة، ولا إعادة حساب |
| 6 | عملة `ReceivingAccount` | النموذج بلا حقل عملة — تُقرأ بعملة الدفتر. يُراجَع إن استُقبلت أموال بعملات مختلفة |

---

## 22. رقم الربط — مصدره المكتبة العالمية وحده

**المشكلة:** جدول «عمليات المنتجات» كان يعرض عمودين لشيء واحد — `Küpür`
و«رقم الربط» — وأحدهما (`provider_package_id`) معرّف الباقة لدى المزوّد لا جسر
الباقة. وكان صاحب المتجر يكتب الرقم بيده، فرقمٌ مخطوء يقطع الجسر بصمت.

**القاعدة المعتمدة:** رقم الربط (`Product.kupur`) جسر ثابت **مصدره المكتبة
العالمية حصراً**. مالك المنصّة يعرّف في المكتبة: الاسم + رقم الربط + تكلفة
مقترحة + سعر موصى؛ وصاحب المتجر يختار رقماً من المتاح فيُملأ الباقي تلقائياً،
ثم يملك تعديل الاسم والسعرين — **لا رقم الربط**.

| الطبقة | التنفيذ |
|-------|---------|
| `GET /api/catalog/games/<id>/library-packages/` | أرقام المكتبة لهذه اللعبة **ناقص** ما أُخِذ. `linked:false` للعبة لا أصل لها في المكتبة (إدخال يدوي استثناءً) |
| `ProductSerializer.validate` | عند الإضافة: يرفض رقماً خارج المكتبة، ورقماً مكرّراً في اللعبة، ورقماً فارغاً للعبة مستورَدة |
| `ProductSerializer.update` | يُسقط `kupur` دائماً — لا يتغيّر بعد الإنشاء |
| `LibraryProductSerializer.validate` | رقم الربط فريد داخل لعبة المكتبة الواحدة |
| `GameDetail.tsx` | عمود واحد «رقم الربط» (كان عمودين) · قائمة اختيار للأرقام المتاحة تملأ الاسم والسعرين · تحرير الاسم/التكلفة/الموصى **داخل الجدول** · زر حذف يعيد الرقم إلى القائمة · رقم الربط للعرض فقط في نافذة التعديل |
| `Platform.tsx` · `Library.tsx` | «الكوبور» ← «رقم الربط» ويظهر بجانب كل باقة قبل الاستيراد |

**حذف باقة يعيد رقمها إلى القائمة** — وهو السلوك المقصود: عشرة أرقام في
المكتبة، أُضيفت كلّها، حُذفت ثلاث ⇒ تعرض القائمة تلك الثلاثة وحدها.

**تنبيه:** «معرّف الباقة لدى المزوّد» في نافذة التوجيه شيء آخر تماماً — يختلف من
مزوّد لآخر ويُدار من «ربط الباقات». أُعيدت تسميته لئلّا يُخلط بجسر المكتبة.

**9 اختبارات جديدة** في [catalog/tests.py](backend/catalog/tests.py)
(`LibraryLinkNumberTest`) — المجموع 35 خضراء.

---

## 23. قسم الوكلاء — كرة الحالة، وجدول المعطّلين، وشجرة الوكلاء

**كرة واحدة بدل شارة وشريحتين.** عمود «الحالة» كان يحمل شارة «نشط/موقوف»
وشريحتَي «مشتريات/ألعاب» — ثلاث إشارات لا تُقرأ بلمحة. صار كرةً واحدة:
**خضراء = نشط، حمراء = معطّل**، وهي الزرّ نفسه:

- ضغط الخضراء ⇒ يُعطَّل الوكيل **ويغادر الجدول فوراً**.
- زرّ **«إظهار المعطّلين (N)»** فوق الجدول يقلبه إلى جدول المعطّلين وحدهم.
- ضغط الحمراء هناك ⇒ يعود إلى القائمة الأولى.

الجدول الأول لم يعد يعرض المعطّلين إطلاقاً، فسقط خيار «موقوف» من شريط الفلترة
(بقي «الكل» و«رصيد سالب») — لأن مكانه صار زرّاً مستقلاً.

**عمود «الرقم» صار تسلسلياً.** كان يعرض `login_id` النصّي (`bayi003`). أُضيف
`User.dealer_no`: رقم تسلسلي **داخل كل متجر** يبدأ من 1، تُرقّم به الهجرة
[0020](backend/core/migrations/0020_user_dealer_no.py) الوكلاء القائمين بترتيب
إنشائهم، ويأخذ الوكيل الجديد التالي له. `login_id` باقٍ كما هو — **لأنه كلمة
الدخول نفسها، وتغييره يقطع دخول وكلاء يعملون اليوم** — ويظهر في تلميح العمود
وفي نافذة الإعدادات.

**صفحة «أسعار الوكلاء» حُذفت** (`/oyunpin/dealer-prices` + ثلاث نقاط في
`catalog`). ما يلزم منها انتقل إلى **إعدادات الوكيل ← الحساب**:

| كان في الصفحة المحذوفة | صار |
|---|---|
| حد تحميل الألعاب | قائمة في إعدادات الوكيل |
| مجموعة الأسعار (للعرض فقط) | قائمة **قابلة للتحرير** في إعدادات الوكيل |
| إذن IP خارجي | **حُذف** — كان بلا أثر في طلبات الـ API، وتحذيره يقوله بنفسه |
| تعيين مجموعة جماعياً | سقط مع الصفحة؛ التعيين لكل وكيل من إعداداته |

**صفحة جديدة: «وكلاء تحت وكيل»** (`/dealers/sub`) — جدول لكل وكيل كبير: رصيده
في الترويسة ومجموع أرصدة دكاكينه، وتحته دكاكينه. تُغذّيها
`GET /api/dealers/hierarchy/` (صاحب المتجر وحده). الدكاكين التابعة لوكيل لم يعد
كبيراً تظهر في جدول «معلّقة» بدل أن تُبتلع صامتةً.

**8 اختبارات جديدة** في [core/tests.py](backend/core/tests.py) (`DealerListTest`):
الترقيم التسلسلي · الرقم التالي عند الإنشاء · التعطيل والإعادة · حفظ المجموعة
وحدّ التحميل · رفض مجموعة غير موجودة · الشجرة ومجاميعها · منع غير المالك ·
اختفاء نقاط «أسعار الوكلاء». المجموع **47 خضراء**.

---

## 24. شجرة الوكلاء في جدول واحد

**التصحيح:** «قائمة الوكلاء» كانت تفلتر `role=BAYI` فقط، فالوكيل الكبير **لا يظهر
لصاحب المتجر أصلاً** — يراه في لوحته من يدخل بحسابه فقط. والصفحة المستقلّة
«وكلاء تحت وكيل» قسمت القراءة على مكانين.

**الشكل المعتمد — قسم واحد:**

- الجدول يعرض **الكبار والصغار معاً**. الوكيل الكبير: **نجمة ★ ذهبية** بجانب
  اسمه (تلميحها «وكيل كبير»)، واسمه بلون ذهبي داكن، وعدد دكاكينه بين قوسين.
- **الدكان التابع لكبير لا يظهر صفّاً مستقلاً** — مكانه داخل صفّ وكيله.
- الضغط على اسم الكبير يفتح تحته **صندوقاً بإطار ذهبي**: ترويسته «دكاكين فلان»
  ورصيده، وجدوله دكاكينه (رقم · اسم · رصيد · حد ائتماني · كرة الحالة · كشف
  حساب وإعدادات).
- صفحة `/dealers/sub` وقائمتها ونقطة `GET /dealers/hierarchy/` **حُذفت كلّها**.

**صار للشجرة مقبض في الواجهة** (لم يكن لها من قبل): في **إعدادات الوكيل** قسم
«نوع الوكيل وموقعه في الشجرة» — قائمة النوع (وكيل / ★ وكيل كبير) وقائمة «يتبع
الوكيل الكبير»، ومثلهما في نموذج الإضافة. الحواجز: الكبير لا يتبع كبيراً، ولا
يُخفَّض وتحته دكاكين، ولا يتبع الوكيل نفسه.

**بذرة حيّة:** `seed_demo` صار ينشئ **أحمد العلي** وكيلاً كبيراً (`5553333333` /
`ahmad123`) وتحته «محل النور» و«محل الأمانة» (`shop123`)، ويُرقّم أي وكيل قديم
بلا `dealer_no`.

**7 اختبارات جديدة** (52 خضراء): التعشيش تحت الكبير · بقاء المستقلّ صفّاً ·
الترقية إلى كبير تفكّ تبعيته · منع تخفيض كبير تحته دكاكين · ضمّ دكان لكبير ·
منع كبير من اتّباع كبير · إنشاء كبير وتابع له.

### ⏸ ما لم يُنفَّذ بعد — بانتظار قرار المالك

المسار المالي للوكيل الكبير (خصم طلبات الدكاكين من محفظته + مجموعات أسعار
خاصّة به يبيع بها لدكاكينه) **لم يُمَسّ**. الوضع اليوم: تُخصم من محفظة الدكان
نفسه، وربح الكبير يأتي من **هامش مئوي لكل باقة** (`AgentMargin`) لا من مجموعات
أسعار. تغييره يمسّ دفتراً حيّاً، فلا يجوز التخمين فيه — الأسئلة معلّقة في
المحادثة.

---

## 25. المسار المالي للوكيل الكبير — دفتران لا دفتر

**القرار (من المالك):** دكان الوكيل الكبير **لا يشتري من المتجر**. يشتري من
وكيله، والوكيل يشتري من المتجر، وربح الوكيل يدخل محفظته **لحظة الطلب**.

### الأرقام على طلب واحد

| الطرف | الحركة | المثال |
|------|--------|--------|
| الدكان | −`buyer_price` | −10 |
| الوكيل الكبير | +`buyer_price` ثم −`sell_price` | +10 ثم −8 = **+2** |
| المتجر | يقبض `sell_price`، ربحه `sell_price − cost_price` | 8 − 6 = 2 |

**ترتيب الساقين مقصود:** القبض قبل الدفع في الحالتين (تنفيذاً ونقضاً)، فلا يوقف
الحدُّ الائتماني عمليةً محصّلتها في صالح المحفظة.

### حقول جديدة في `Order`
- `agent` — الوسيط (فارغ = لا وسيط).
- `buyer_price` — **ما دفعه المشتري**. هجرة `0004` تملؤه من `sell_price` للطلبات
  القديمة، وإلّا قرأ كل وكيل «دفعتَ 0.00» في تاريخه كلّه.
- `agent_profit` — فرق السعرين.

`sell_price` بقي **ما قبضه المتجر** لا ما دفعه الدكان — كل تقارير الإيراد
(`Sum("sell_price")`) تعتمده، ولو صار سعر الدكان لتضخّمت إيرادات المتجر بربح
الوكيل.

### التسعير
- **تكلفة الوكيل الكبير** = سعر مجموعته عند صاحب المتجر (كما هو).
- **سعر الدكان** = سعر الباقة في `AgentPriceGroup` التي وضعه وكيله فيها.
  سلسلة الارتداد: مجموعة الوكيل ← هامشه المئوي القديم (`AgentMargin`) ← تكلفة
  الوكيل نفسها. **لا يُباع الدكان بأرخص ممّا اشترى به وكيله.**
- مجموعة الدكان عند *صاحب المتجر* تُتجاهَل تماماً إن كان تابعاً — «أنا أبيع أحمد
  بسعري، وأحمد يبيع دكاكينه كما يشاء».
- تسعير أقلّ من تكلفة الوكيل **يُرفض** من الـ API: بيعٌ بخسارة صامتة لا يريده أحد.

### نماذج وواجهات
- `AgentPriceGroup` (وكيل × اسم) + `AgentProductPrice` (مجموعة × باقة × سعر)
  + `User.agent_price_group`.
- **لوحة الوكيل الكبير**: تبويب **«مجموعات أسعاري»** (إنشاء/حذف · شرائح ملوّنة ·
  تسعير كل باقة inline مع عمودَي «تكلفتي» و«ربحي») + عمود **«مجموعة أسعاره»**
  في جدول دكاكينه. وتبويب «هامش سريع» بقي كطريق مختصر للدكان بلا مجموعة.
- **تصحيح**: لوحة الوكيل الكبير كانت تعرض `Order.profit` — وهو **ربح صاحب
  المتجر** — على أنه ربحه هو، و`sell_price` على أنه ما دفعه دكانه. صارا
  `agent_profit` و`buyer_price`.

**13 اختباراً جديداً** (63 خضراء): الساقان الثلاث · تجاهل مجموعة المتجر للتابع ·
المستقلّ يشتري من المتجر · الدكان بلا مجموعة يشتري بالتكلفة · الإلغاء يعكس
الساقين · إعادة القبول تُعيدهما · استقلال ربح الوكيل عن تكلفة المزوّد · API
المجموعات (إنشاء/تسعير/ربط) · رفض السعر دون التكلفة · منع المساس بدكان وكيل آخر ·
منع الدكان من إدارة المجموعات · أرقام اللوحة.

---

## 26. لا كتابة يدوية لرقم الربط — ربط اللعبة بنظيرتها

**العطل:** ألعاب المتاجر أُنشئت من `seed_catalog` بأسماء تخالف المكتبة
(«PUBG Mobile» مقابل «PUBG Mobile UC») وبلا `master_library_uuid`، فكان الارتداد
إلى حقل كتابة يدوية — وهو نقض القاعدة نفسها.

**الحلّ:** حقل الكتابة اليدوية **حُذف من الواجهة**، ومحلّه أحد أمرين:

1. **لعبة لها نظير:** قائمة الأرقام المتاحة كما هي.
2. **لعبة بلا نظير:** شريط «اربطها بلعبة من المكتبة» — قائمة بألعاب المكتبة
   (مع عدد باقاتها) وزرّ ربط. بعده تنفتح قائمة الأرقام.

**مطابقة الاسم تلقائياً** (`_library_game_for`): تطابقاً بعد تجريد المسافات
وحالة الأحرف، ثم احتواءً **وحيداً**. تطابُقان محتملان ⇒ لا تخمين، يختار المالك.
وعند العثور يُثبَّت `master_library_uuid` فلا يتكرّر البحث.

**والأهمّ — تبنّي الأرقام** (`_adopt_kupur_from_library`): عند الربط تنال باقات
المتجر أرقامَها من المكتبة **بمطابقة الاسم**. بدونه تُقرأ لعبةٌ فيها «60 UC» بلا
رقم وكأن الرقم 60 ما زال متاحاً، فيُضاف ثانيةً وتصير باقتان لباقة واحدة. لا
يُلمَس رقمٌ موضوع، ولا يُمنَح رقم أخذته باقة أخرى.

**5 اختبارات جديدة** (70 خضراء): المطابقة بالاسم تربط وتمنح الأرقام · اللعبة بلا
نظير تُرجع قائمة المكتبة · الربط اليدوي يمنح الأرقام · التبنّي لا يدهس رقماً
موجوداً · نظيران محتملان يبقيان بلا ربط.

---

## 27. لوحة الوكيل الكبير — هيكل لوحة صاحب المتجر بثلاثة أقسام

اللوحة القديمة كانت صفحةً مستقلّة بتبويبات خاصّة (`/bigagent` وحدها). صارت
**نفس `AdminLayout`** — الهيدر والقائمة والألوان والجداول — لكن بأقسام الوكيل:

| القسم | الصفحات |
|------|---------|
| الرئيسية | `/bigagent` — رصيده · دكاكينه · طلباتهم الناجحة · **ربحه هو** + جدول دكاكينه + تنبيه بالسالبين |
| الألعاب | `/bigagent/orders` (متابعة الطلبات) · `/bigagent/price-groups` (مجموعات الأسعار) |
| الوكلاء | `/bigagent/dealers` (قائمة الوكلاء) |

`AdminLayout` صار **يقرأ الدور**: `AGENT_TABS` بدل `ADMIN_TABS`، قائمة فرعية
خاصّة، و**شريط التنبيهات مخفيّ** له (عدّادات صاحب المتجر لا تعنيه، ولا يُنادى
`/alerts/` أصلاً). ما لا يملكه لا يُعرض له: لا كتالوج ولا مزوّدين ولا مخزون ولا
إعدادات متجر ولا تقارير.

**`/store` صار للدكاكين وحدها.** كان الوكيل الكبير يصله بزرّ «بيع مباشر» — وهو
لا يشحن من الموقع، إنما يدير من تحته.

### شحن الدكاكين — حوالة لا هبة
`POST /api/agent/dealers/<id>/wallet/` — يشحن الوكيل دكانه أو يسحب منه، والمبلغ
**يتحرّك بين المحفظتين في معاملة واحدة**: يُخصم من محفظته ويُضاف إلى محفظة دكانه.
لا يطبع رصيداً من العدم؛ يوزّع ما اشتراه من صاحب المتجر. وما يتجاوز رصيده يُرفض
بلا ساق نصفية. ومعه `GET …/statement/` لكشف حركات الدكان بعملة عرض الوكيل.

> قبل هذا لم يكن لدكاكين الوكيل الكبير مصدر رصيد إلا صاحب المتجر مباشرةً — وهو
> نقض لفكرة الوسيط.

**تبويب «هامش سريع» أُسقط** من الواجهة (حلّت محلّه المجموعات)، وبقي `AgentMargin`
في المنطق كارتداد للدكان الذي لم يُوضع في مجموعة.

**6 اختبارات جديدة** (76 خضراء): الشحن يحرّك المحفظتين · السحب يعكسهما · لا شحن
بما لا يملك · لا مساس بدكان وكيل آخر · الكشف يعرض حركات الدكان · رفض مبلغ صفري
أو سالب.

**تحديث (نفس اليوم):** «متابعة الطلبات» انتقلت إلى قسم **الألعاب** باسمها
المعتاد (كانت «طلبات وكلائي» تحت الوكلاء) — ليطابق ترتيب لوحة صاحب المتجر.
والصفحة نالت: بحثاً (إيصال/دكان/باقة/لعبة) · فلتر دكان · تحديثاً تلقائياً كل
دقيقة وزرّ تحديث · وثلاثة مؤشّرات فوق الجدول (عدد الطلبات · ما دفعوه له · ربحه
منها) تتبع الفلترة لا القائمة كاملةً.

**عن فرع Render:** لا مفتاح API ولا CLI في المشروع، فلوحة Render تحتاج حساب
المالك ولا سبيل إليها من هنا. والحلّ العملي المعتمد: **كل دفعة تذهب إلى الفرعين
معاً** (`main` والفرع الذي يبني منه Render)، فيبقى الموقع محدَّثاً بلا أي خطوة
على المالك. التبديل تنظيميّ لا أكثر.

---

## 28. «بيانات الدخول غير صحيحة» بعد تغيير كلمة سرّ وكيل

**التشخيص:** مسار الخادم سليم — اختبارٌ يُثبت أن تغيير السرّ من نافذة الإعدادات
ثم الدخول به ينجح، والقديم يُرفض بـ 401. فالخلل في الطريق إليه، ومصيدتان:

1. **حقل التأكيد الفارغ:** النموذج كان يشترط تطابق الحقلين، فإن كُتب السرّ في
   الأوّل وحده لم يُرسَل `new_password` **وظهرت رسالة «حُفظت الإعدادات» نفسها**
   — فيظنّ المالك أنه غيّره وهو لم يتغيّر.
2. **مسافة في رقم الدخول:** نسخٌ ولصقٌ يجرّ مسافةً، فيفشل الدخول برسالة «بيانات
   الدخول غير صحيحة» المبهمة.

**المعالجة:**
- الخادم يُرجع `password_changed: true/false`، والواجهة تقولها صراحةً:
  «حُفظت الإعدادات — وكلمة السر الجديدة فعّالة الآن لرقم الدخول 5553333333»،
  أو «حُفظت الإعدادات (لم تتغيّر كلمة السر)».
- النموذج يرفض الحفظ إن كان التأكيد فارغاً أو غير مطابق أو السرّ أقصر من 5.
- `LoginSerializer` يشذّب `login_id` (ولا يمسّ كلمة السر — مسافتها قد تكون منها).
- قسم كلمة السر يعرض **رقم الدخول** بخطّ مونوسبيس مع زرّ نسخ.

**4 اختبارات جديدة** (80 خضراء): السرّ الجديد يعمل والقديم يُرفض مع الإقرار ·
الحفظ بلا سرّ يقولها · رقم دخول بمسافات يعمل · السرّ القصير مرفوض ولا يُبطل القديم.

---

## 29. التوجيه الداخلي بلا إذن — متجرٌ ينفق رصيد وكيلٍ ليس له

**الاكتشاف (2026-08-15):** بعد إنشاء المستأجر الثاني `islam`، جُرِّب المزوّد
الداخلي برقم الدخول `bayi003` — وهو وكيل بشريّ عند `alaya` برصيد ~25 ألفاً، لا
علاقة له بإسلام. **فجلب الرصيد بنجاح.**

**الجذر:** `InternalTenantAdapter` كان يتحقّق من شرطين فقط: أن الحساب موجود
بدور وكيل، وأن متجره **يختلف** عن متجرنا. ولا يتحقّق أبداً أنّ الحساب **يخصّنا**.
و`login_id` فريد عبر المنصّة كلّها ([core.User](backend/core/models.py)) وسهل
التخمين. فأيّ صاحب متجر يضيف مزوّداً داخلياً برقم دخول وكيلٍ عند متجرٍ آخر،
ثم يشتري من محفظته: الطلب يُنشأ هناك باسمه، وتُخصم محفظته، بلا علم أحد.

كان غير مؤذٍ ما دام المستأجر واحداً — وصار حقيقياً بوجود اثنين.

**لماذا لا يصلحه توكن:** التوكن يجيب «من أنت؟» عبر الشبكة، والمتجران هنا في
قاعدة واحدة ولا شبكة بينهما — إسلام يعرف هويّته أصلاً. المفقود «هل يحقّ لك؟»،
وجوابه **إذن** لا هويّة. (أمّا API للخارج بتوكن فميزة أخرى غير مبنيّة أصلاً:
المنصّة تستهلك توكن ZDK ولا تصدّر واحداً.)

**المعالجة:** حقل واحد — `User.internal_supply_allowed` (هجرة `core.0022`)،
**مطفأ افتراضياً**. يضعه صاحب المتجر المورّد بيده من إعدادات الوكيل ← قسم
«التوريد لمتجر آخر على المنصّة». وفُرِض في **المداخل الثلاثة** لا في الطلب وحده
(`get_balance` · `list_packages` · `place_order`) عبر `_resolve_dealer()` —
لأن قراءة رصيد وكيلٍ وكتالوجه تسريبٌ كذلك، وهي بالضبط ما نجح في التجربة.

**7 اختبارات جديدة** (87 خضراء): الثغرة الأصلية مسدودة في الرصيد والكتالوج ·
الطلب مرفوض **ولا يُخصم قرش ولا يُنشأ طلبٌ عند المورّد** · سحب الإذن يوقف
مزوّداً كان يعمل · المأذون يعمل كما كان · الإذن لا يُلغي منع التوجيه للمتجر
نفسه · رقم دخول مجهول مرفوض.

> **ملاحظة تشغيلية:** أي مزوّد داخلي قائم يتوقّف حتى يفعّل المورّد الإذن —
> والرسالة تقول ذلك حرفاً. وأُضيفت `config.settings_sqlite_test` لتشغيل
> الاختبارات محلياً بلا Postgres: `manage.py test --settings=config.settings_sqlite_test`.

---

## 30. واجهتنا الخارجية — كي يشتري منّا متجرٌ من خارج المنصّة

**الطلب (2026-08-15):** متاجر كثيرة تريد الربط بنا. كان لدينا مزوّد داخلي
(متجر ← متجر على منصّتنا) وكنّا نستهلك API غيرنا — ولم نكن **نصدّر** واحداً.

### القرار: ننسخ شكل ZDK حرفاً
كل متجر في السوق مربوطٌ ببركات أو أب‑ستور، وكودهم مكتوب لترويسة `api-token`
ولمسارات `/client/api/…`. فالنسخ يجعل ربطهم بنا **تبديلَ رابطٍ وتوكن** لا
إعادةَ كتابة. شكلٌ من عندنا كان سيكلّف كلَّ واحد منهم مبرمجاً وأسبوعاً، وأكثرهم
لن يفعل. الوثيقة الكاملة: [docs/integrations/OUR_API.md](docs/integrations/OUR_API.md).

### التوكن للوكيل لا للمتجر
لأن المحفظة على الوكيل. فالطلب الوارد يمشي في نفس مسار ضغطة «شراء» في لوحته:
سعر مجموعته · خصم محفظته · توجيه المتجر. **لا دفتر جديد ولا منطق مالي جديد** —
وهو ما جعل الميزة أصغر بكثير ممّا تبدو.

### الإذن بيد صاحب المتجر — مغلقاً افتراضياً
كان تصميمي الأوّل: يولّده الوكيل بنفسه بلا إذن، لأن المفتاح لا يفتح إلا محفظته
هو وحدُّه الائتماني سقف الضرر كما في لوحته. **وقرّر المالك خلافه (2026-08-15)**
ومعه حقّ: الـ API لا يمنح صلاحيةً جديدة، لكنه يجعل **إعادة البيع بالجملة آليةً
بلا كلفة** — والوكيل الذي يردعه الجهد يدوياً لا يردعه شيء آلياً. فالقرار تجاريّ
لا تقنيّ، ومكانه يد صاحب المتجر.

`User.api_access_allowed` (هجرة `core.0023`) **مغلق افتراضياً**، يفتحه صاحب
المتجر من إعدادات الوكيل ← «الربط الخارجي (API)».

- **يُفحص عند كل نداء** لا عند التوليد: سحبُه يوقف ربطاً قائماً **فوراً**. لو
  فُحص عند التوليد لبقي ربطٌ سُحب إذنه يشتري إلى الأبد — لأن أحداً لن يولّد
  مفتاحاً بعد السحب.
- **السحب يحتفظ بالمفتاح**، فإعادة الإذن تُحيي الربط بلا إعادة ضبطٍ عند الجهة
  الخارجية.
- **كود مستقلّ `123`** لا `122`: «موقوف» يعالجه الوكيل و«غير مأذون» يعالجه
  صاحب المتجر — تشخيصان مختلفان لمبرمج الجهة الخارجية.
- الوكيل غير المأذون يرى **شرحاً هادئاً** («راسل صاحب المتجر») لا صفحة خطأ،
  ولا يُولَّد له مفتاح أصلاً.

أمّا **التوليد** فبيد الوكيل بعد الإذن — المفتاح لا يفتح إلا محفظته هو. (بخلاف
«مأذون بالتوجيه الداخلي» في §29 الذي يمسّ محفظة متجرٍ آخر.)

### منع الشحن المزدوج — الحارس الذي لا يُتنازل عنه
الشبكة تنقطع **بعد** الشحن و**قبل** وصول الردّ، فيعيد العميل المحاولة فيُشحن
اللاعب مرّتين. `order_uuid` مطلوب، ويُحفظ في `Order.client_uuid` تحت **قيد فريد
في القاعدة** على `(dealer, client_uuid)` — النطاق الوكيل لا المتجر، وإلّا اصطدم
وكيلان بمعرّف واحد وتلقّى الثاني طلب الأوّل جواباً.

طبقتان والثانية هي الملزِمة: فحصٌ قبل الإنشاء يعيد الطلب الأوّل مع
`duplicate: true`، وقيدُ القاعدة يمسك النداءين المتوازيين اللذين يمرّان معاً من
الفحص — فتنسحب معاملة الخاسر بخصمها ويُعاد له **طلبُ الفائز لا خطأ**. قوله خطأً
كان يدفع العميل إلى معرّف جديد فشراءٍ ثانٍ.

والتنفيذ خارج معاملة الإنشاء: يخرج إلى الشبكة، وإبقاء المعاملة مفتوحة طواله
كان يقفل صفّ المحفظة ثلاثين ثانية.

### تفصيلان يستحقّان التذكّر
- **«العالق» يخرج `wait` لا `reject`**: فشل لدى كل المزوّدين وينتظر الأدمن،
  **والمال لم يُسترجع**. قوله `reject` كان يدفع الخارجيَّ إلى إعادة الطلب على
  شحنةٍ قد تُنفَّذ يدوياً بعد قليل.
- **`qty>1` مرفوض صراحةً (109)** لا متجاهَل صامتاً — المنتج عندنا باقة واحدة،
  والتجاهل كان يبيعه واحدة ويحاسبه على ظنّ ثلاث.

### الصفحة
**إعداداتي ← الربط الخارجي (API)** في لوحة الوكيل — لا تبويباً مستقلاً في
الشريط: هو إعدادٌ يُضبط مرّةً ثم يُنسى، وتسعة تبويبات في الشريط كثير. ورابط
`/store/api` يبقى صالحاً ويفتح على القسم مباشرةً (ويُبرز «إعداداتي» في الشريط).
يفتح ببطاقة **«بيانات الربط»**
وحدها فيها كل ما يُسلَّم لمبرمج الجهة الخارجية:

- **المفتاح** مخفيّاً (`abc123••••…xyz9`) وزرّا إظهار ونسخ — فلا يقع في لقطة
  شاشة أو مشاركة عرضٍ بلا قصد.
- **المفتاح ثابت**: يُنشأ مرّةً ولا يتغيّر بزيارة ولا بإعادة دخول ولا بمرور
  وقت. يُسلَّم مرّةً ولا يُراجَع. يحرسه اختبار.
- **الروابط الكاملة الجاهزة للنسخ** — الرصيد · المنتجات · إرسال طلب · استعلام
  بالرقم · استعلام بمعرّف العميل — كلٌّ بزرّ نسخ، وما بين `{ }` يملؤه العميل.
  كانت المسارات مكتوبة نسبيّةً (`/client/api/profile`) فيبقى على القارئ أن
  يركّبها بنفسه.
- **زرّ تبديل المفتاح نُقل إلى آخر الصفحة** بتأكيد صريح: ليس خطوةً من خطوات
  الإعداد بل زرَّ طوارئ عند التسريب، ووضعه بجانب المفتاح كان يغري بالضغط.

ثم التوثيق كاملاً **بتوكنه هو داخل الأمثلة** لا `YOUR_TOKEN` — وهذا الفرق
الصغير هو ما يجعلها تُنسخ وتعمل من أول مرّة بدل أن تُقرأ ثم تُترك.

**39 اختباراً جديداً** (126 خضراء) + **فحص دخان حيّ** بخادم فعلي ونداءات HTTP
فعلية: توكن ناقص → 120 · رصيد · كتالوج · طلب · إعادة الطلب بنفس المعرّف عشر
مرّات → **طلب واحد وخصم واحد** · استعلام بالـ uuid · عزل الوكلاء والمتاجر.

> **لم يُجرَّب مع جهة خارجية حقيقية بعد.** ما يلزم تأكيده مع أوّلهم في §8 من
> [OUR_API.md](docs/integrations/OUR_API.md) — وأهمّه: هل يحفظون `order_uuid`
> قبل الإرسال أم يولّدونه عند كل محاولة (الخطأ الشائع الذي يبطل الحماية كلّها).


---

## 31. التوجيه الداخلي بعد أول تجربة حيّة — ثلاثة أعطال

**التجربة (2026-08-15):** دكانٌ تابع لمتجر `islam` طلب باقة → إسلام وجّهها آلياً
إلى `alaya` → علايا وافقت **يدوياً**. فبلّغ المالك عطلين، وتحته ثالث أخطر.

### 🔴 العطل 1 — الطلب يتجمّد «قيد التنفيذ» أبداً
`InternalTenantAdapter` لم يكن يعرّف `fetch_status`، فالمحوّل الأساسي يردّ
`unsupported` وتترك حلقةُ المراقبة الطلبَ كما هو. أي أن موافقة المورّد اليدوية
**لا يحمل خبرها شيء** إلى المشتري ولا إلى دكانه. صمتٌ لا عطلٌ ظاهر: اللوحة تقول
«يراقب 1 طلباً» وهي لا تراقب شيئاً.

**العلاج:** `fetch_status` يقرأ طلبنا لدى المورّد من قاعدتنا (بلا شبكة) عبر
المرجع `tenant:<subdomain>:<رقم الفيش>`. ناجح ⇒ ناجح + PIN · ملغى ⇒ فشل (فتلغي
الحلقة وتسترجع، لأن المورّد ردّ المال إلى محفظتنا لديه) · **عالق ⇒ انتظار**
لا فشل، فمالُه محجوز هناك ولم يُستَرجع لنا.

### 🔴 العطل 2 — تكلفةٌ موهومة وربحٌ كاذب
المحوّل كان يُرجع التكلفة في فرع **النجاح وحده**. والتنفيذ اليدوي يعني
`processing`، فيبقى في دفترنا `cost_price` المكتوب على المنتج (0.85$) بينما
خُصم من محفظتنا لدى المورّد 32.00$ فعلاً — ربحٌ ظاهره 0.15$ وحقيقته خسارة 31$.

**العلاج:** التكلفة تُعتمد **لحظة الإنشاء**، لأن المال يخرج حينها لا عند النجاح.
ومصدرها `sup_order.buyer_price` لا `sell_price`: الأوّل ما خُصم من محفظتنا،
والثاني ما قبضه المتجر المورّد — ويفترقان إن كان حسابنا لديه تحت وكيل كبير.

### 🔴 العطل 3 — القسمة على الليرة (لم يظهر بعد، وكان سيظهر)
`_apply_real_cost` يمرّر كل تكلفة على `currency.from_provider` — محوّلٌ مبنيّ
لمزوّدين يسعّرون بالليرة. والمزوّد الداخلي يسلّم الرقم **بعملة دفترنا أصلاً**،
فكانت 32.00$ ستُقسم على 41.5 فتصير 0.77$ — أي إظهار الطلب رابحاً وهو خاسر
بأربعين ضعفاً. لم يظهر في التجربة لأن العطل 2 منع وصول التكلفة أساساً.

**العلاج:** `ExecutionResult.cost_is_base` — المحوّل يقول إن رقمه بعملة الدفتر
فلا يُحوَّل. والمحوّل الداخلي يعبر حدود العملة بنفسه (`_to_our_base`)، فمتجران
بدفترين مختلفين يتبادلان أرقاماً صحيحة.

### 🟠 وزيادةً: حماية الخسارة صارت تسبق أوّل طلب
كانت الحماية تعتمد سعراً **متعلَّماً** من طلب سابق (`ProductLink.extra.price`)،
فأوّل طلب على كل باقة يمرّ بلا حماية — «أول طلب يحدّده». هذا مقبول لمزوّد
خارجي (سؤاله يكلّف نداء شبكة)، لكنه بلا مبرّر للداخلي: **سعره في قاعدتنا**.

أُضيف `BaseAdapter.quote()` — سعرٌ حيّ يقينيّ يعيده من يستطيع، و`None` من لا
يستطيع. الداخلي يعيده، فتمنع الحمايةُ الخسارةَ **قبل** الطلب الأوّل لا بعده.

> ⚠️ **أثرٌ فوري على الإعداد القائم:** توجيه إسلام الحالي (بيع 1.00$ · شراء
> 32.00$) صار يُحجَب بحماية الخسارة ويصير الطلب «عالقاً» بدل أن يُنفَّذ. هذا
> هو الصواب — كل طلبٍ كان يخسر ~31$ صامتاً — والعلاج تسعيرٌ صحيح لا إطفاء
> الحماية.

**8 اختبارات جديدة** (134 خضراء): الموافقة اليدوية تصل · التكلفة الحقيقية فوراً
· لا قسمة على الليرة · عبور حدود العملة (32 ل.ت ⇒ 0.77$) · الحماية تمنع قبل
الأوّل وتمرّر الرابح · إلغاء المورّد يسترجع لوكيلنا · عالق المورّد لا يسترجع.


---

## 32. جلسة القرارات الأربعة — قفلٌ واشتراكٌ وبطاقاتٌ وقائمة أسعار

بعد جرد [plan2.md](plan2.md) اختار المالك أربعة بنود ونُفّذت في جلسة واحدة.

### ١. قفل الدخول بعد ثلاث محاولات
`failed_login_count` كان **يُعدّ ولا يُقرأ أبداً** — تخمينُ كلمات السرّ مفتوحٌ
بلا حدّ على حساباتٍ تملك محافظ.

- **القفل يُفحص قبل كلمة السرّ.** لو فُحص بعدها لبقيت المحاولة تُختبَر فعلياً
  على حسابٍ مقفل، فيبقى التخمين ممكناً وإن لم يُفتح الباب.
- **المخرج الوحيد كلمةُ سرٍّ جديدة** من فوقك. زرُّ فتحٍ بلا تبديل كان يعيد
  الحساب بكلمةٍ ثبت أن أحدهم يخمّنها.
- **الرسالة تعدّ ما بقي** («تبقّت 2 محاولة») — القفل بلا إنذار يفاجئ صاحب
  الحساب وهو يظنّ أنه يخطئ الكتابة.
- **`manage.py unlock_user`** لمالك المنصّة: لا أحد فوقه، فلو قُفل حسابه
  لأُغلقت المنصّة بلا مفتاح.

### ٢. الاشتراك: مهلة سماح ثم منع الشراء
كان `sub_expires_at` يُحفظ ويُعرض **ولا شيء يفحصه**. صار `subscription_state()`
مرجعاً واحداً بأربع حالات، والبوّابة **في `create_order` وحدها** — لأن كل شراء
يمرّ منها: المتجر واللوحة والواجهة الخارجية والتوجيه الداخلي. حراستُها في كل
مدخل على حدة كانت تترك باباً منسيّاً.

ومالك المنصّة يضبط أيام السماح وتاريخ الانتهاء يدوياً، و**يُعفي متجراً من المنع
أصلاً** (`sub_enforce`) — بدونه كان الإعفاء يتطلّب تمديداً وهمياً للتاريخ.

> **عطلٌ أمسكه اختبار:** `timezone.now().date()` يعطي تاريخ **UTC** والمتجر
> يعيش بتوقيت إسطنبول، فقد ينتهي الاشتراك عنده ونحسبه سارياً ساعات. صار
> `timezone.localdate()` في المواضع الأربعة.

### ٣. البطاقات — نموذج واحد لسلسلتين
`HomeCard`: مالك المنصّة يكتب لأصحاب المتاجر (وله توجيه بطاقة لمتجر بعينه)،
وصاحب المتجر يكتب لوكلائه. السلوك واحد، وما يتغيّر من يكتب ومن يقرأ — فالخادم
يستنتج الجمهور من **دور الكاتب** ولا يقبله من الواجهة.

الألوان على البطاقة نفسها لا في سمة عامّة: تنبيهٌ أحمر وترحيبٌ هادئ لا يجمعهما
لون واحد. ومعها معاينةٌ حيّة — لا يُحفظ لونٌ لم يُرَ.

### ٤. قائمة الباقات للوكيل
قسمٌ داخل «البيع» لا تبويباً تاسعاً (الشريط ثمانية بطلب المالك). ثلاثة أعمدة:
سعر شرائه هو · سعر التوصية · **رقم المنتج عندنا** بزرّ نسخ — وهو ما يضعه في
`newOrder/{الرقم}/params`، فيتّصل القسم بصفحة الربط الخارجي.

**ورقم الباقة لدى مزوّد المتجر لا يُعرض بحال** — سرٌّ تجاري يدلّ الوكلاء على
مصادر صاحب المتجر. سألتُ المالك أيّ رقم يريد قبل البناء، ويحرسه اختبار.

### وأمّا SMS فمؤجّلة
لا سطرَ إرسالٍ واحد في المشروع، والبناء مؤجَّل بقرار المالك. لكن الصفحة كانت
**توهم صاحبها أن رسائله تصل**، فأُضيفت لافتة صريحة: التأجيل قرارٌ، والإيهام عطل.

**٢٧ اختباراً جديداً** (161 خضراء): القفل بأشكاله السبعة · الاشتراك بحالاته
السبع · البطاقات ومن يكتب لمن (٩) · قائمة الباقات وعدم تسريب أرقام المزوّدين (٤).


---

## 33. حماية الخسارة بعد التجربة الثانية — تُري ولا تُخفي، وتُعصى إن أُصرّ

الحماية عملت فعلاً: طلبُ إسلام (بيع 1.00 · شراء 32.00) حُجب ولم يخرج إلى علايا.
فكشفت التجربة ثلاثة عيوب في **كيف** تحجب لا في **أن** تحجب.

### ١. الحجب كان يُخفي سبب الحجب
الطلب المحجوب بقي يحمل التكلفة المقدَّرة على المنتج (0.85$)، فيقرأ صاحب المتجر
في جدوله **«ربح 0.15»** على طلبٍ حُجب لأنه **يخسر 31**. الرقم الذي هو سببُ
الحجب كان غائباً عن الشاشة.

**العلاج:** عند الحجب تُثبَّت التكلفة الحقيقية على الطلب وإن لم يُرسَل، فيظهر
الربح **سالباً** بحجم الخسارة. لا إرسال ولا خصم — لكن الرقم مرئيّ.

### ٢. الطلب العالق كان بلا مخرج
`dispatch_to_provider` (التوجيه اليدوي) كان يمرّ على الحماية نفسها، فمخرجُ
الطلب العالق الوحيد تعديلُ التسعير. ووظيفة الحماية أن **توقف وتنبّه** لا أن
تمنع إلى الأبد: فإن رأى صاحب المتجر الخسارة ثم وجّه بيده إلى المزوّد نفسه فقد
قرّر — **والنظام لا يعاند صاحبه**.

**العلاج:** `force=True` افتراضياً في التوجيه اليدوي وحده. أمّا `dispatch_order`
التلقائي فيبقى محكوماً بالحماية، وإلّا صارت أوّل إعادة تشغيل تُبطلها.

### ٣. الوكيل كان يرى عيوب متجره
«عالق» تعني أن **توجيه صاحب المتجر** تعثّر: إعدادٌ خاطئ أو مزوّد ساقط أو تسعير
خاسر. ولا حيلة للوكيل في شيء من ذلك، ولا فائدة من إقلاقه به.

**العلاج:** `DEALER_STATUS` يترجمها له **«قيد الانتظار»** — وهو صادقٌ بعينه:
طلبه لم يُحسم بعد. وتبقى «عالق» في لوحة صاحب المتجر وحده. ومعها:
- فلتر «قيد الانتظار» عنده يشمل العالق، وإلّا اختفى طلبه من الفلترين فظنّه ضائعاً.
- عدّاد «قيد الانتظار» في رئيسيته يشمله كذلك.
- وهذا يوافق ما تفعله الواجهة الخارجية أصلاً (§30): العالق يخرج `wait`.

**١٠ اختبارات جديدة** (171 خضراء): التكلفة الحقيقية والربح السالب على المحجوب ·
لا إرسال أثناء الحجب · التوجيه اليدوي يتخطّى · التلقائي لا يتخطّى · الرابح لا
يُمسّ · الوكيل يقرؤها انتظاراً ولا تصله كلمة «عالق» أصلاً · الفلتر والعدّاد ·
وصاحب المتجر ما زال يراها.


---

## 34. لكل مزوّد عملته — الليرة كانت مفروضة على الجميع

**ما لاحظه المالك:** رصيد `znetalaya` مكتوب `$ 100.00` وهو **مئة ليرة تركية**.
ZNET يسعّر بالليرة دائماً، ومزوّدون آخرون يسعّرون بالدولار.

### الجذر: افتراضٌ عامّ في مكان قاعدة
`currency.PROVIDER_CURRENCY = "TRY"` ثابتٌ واحد، و`from_provider()` تقسم على
سعر الليرة **مهما كان المزوّد**. صحيحٌ لـZNET، وخطأٌ بأربعين ضعفاً لغيره:
مزوّدٌ يسعّر بالدولار ويقول «41.50» تُقرأ عندنا **1.00$** — فتبدو الباقة رخيصةً
أربعين مرّة، **وتمرّ حماية الخسارة مطمئنّة** على طلبٍ خاسر.

### العلاج: `Provider.currency`
حقلٌ على المزوّد (هجرة `providers.0002`)، افتراضه `TRY` فلا ينقلب سلوك
المزوّدين المُعدّين سابقاً. ويظهر في نافذة المزوّد **لنوعَي ZNET وZDK وحدهما**:
- **بنك الأكواد والمنفّذ اليدوي** بلا أسعار خارجية أصلاً.
- **المتجر الداخلي** يحوّل بنفسه من دفتر المورّد (§31)، فعملته تُشتقّ لا تُختار.

و`from_provider(tenant, amount, provider)` صارت تأخذ المزوّد. صُحّحت مواضعه
الأربعة: تكلفة الطلب · تحديث التكاليف من الكتالوج · حفظ سعر الربط · الجرد.

### العرض: رقمان في خليّة واحدة
```
100.00 ₺
≈ 2.41 $
```
والسطر الثاني يغيب إن كانت عملة المزوّد عملةَ الدفتر — سطرٌ يكرّر الرقم نفسه
لغوٌ. وإن لم يكن لعملته سعر صرف كُتب **«لا سعر صرف لـEUR»** لا رقمٌ كاذب.

والتحويل يقع **على الخادم** لا في المتصفّح: سعر الصرف مرجعٌ واحد، وحسابُه في
الواجهة يكرّره فيختلف بين شاشة وأخرى.

### ومجموع المزوّدين كان يجمع ليرةً على دولار
`Sum("real_balance")` على الجميع — رقمٌ بلا معنى. صار كلُّ مزوّد يُحوَّل بعملته
قبل الجمع، ومن لا سعر صرف لعملته يُستثنى **ويُقال اسمُ عملته صراحةً**
(`unconverted`) — إسقاطُه صامتاً يجعل المجموع كاذباً بهدوء.

### تصحيحٌ تالٍ: العملة لمن يسعّر بعملته وحده
أوّل لقطةٍ حيّة بعد النشر أظهرت **«₺ 0.00 · ≈ $0.00» تحت بنك الأكواد والمنفّذ
اليدوي** — وهما بلا عملة خارجية أصلاً. فأُضيفت `Provider.prices_in_own_currency`:
الخارجيّ وحده (ZNET · ZDK) يسعّر بعملته، أمّا بنك الأكواد والمنفّذ اليدوي فلا
كتالوج لهما ولا رصيد لدى أحد، والمتجر الداخلي يعبر حدود العملة بنفسه. فعملة
هؤلاء **عملةُ الدفتر** ولا سطر تحويلٍ تحتهم.

ومعها: رصيد المتجر الداخلي صار يُحوَّل من دفتر المورّد كما تُحوَّل تكلفته —
كان يُعرض بعملة المورّد بلا ما يدلّ عليها.

**١٤ اختباراً** (185 خضراء): رصيد الليرة يُظهر مقابله · الدولاري بلا سطر
ثانٍ · عملةٌ بلا سعر صرف تقولها ولا تكذب · المجموع يحوّل قبل الجمع ويسمّي ما
عجز عنه · المزوّد القديم يبقى ليرةً · وتكلفة الطلب تُقسم أو لا تُقسم بحسب
عملة مزوّدها.
