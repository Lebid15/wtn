# realtest.md — دليل الاختبار الحقيقي لتوجيه الطلبات (أنا وأنت معاً)

> الهدف: التحقق سوياً على الموقع الحيّ من **كل مسارات التوجيه**: يدوي · بنك بينات
> (بطاقات الشحن) · متجر داخلي (مستأجر ← مستأجر) · مزوّد خارجي (ZNET / Barakat) ·
> **والتوجيه البديل** (رئيسي → API 1 → API 2 عند الفشل).

---

## 0) ملخّص الآلية (كيف يعمل التوجيه)

- على كل **منتج** (قائمة المنتجات → Pin Listesi) ثلاث قوائم:
  **API القابلة للإرسال** (الرئيسي) · **API 1** · **API 2** (بديلان) + حقل
  **معرّف الباقة لدى المزوّد**. تُحفظ فور تغييرها.
- إن كان تنفيذ المنتج **تلقائياً** وله أي مزوّد: عند الشراء يمرّ الطلب على
  السلسلة بالترتيب — أول نجاح يوقفها؛ **فشل الكل → "عالق"** مع سجلّ محاولات
  كامل يظهر عند الضغط على رقم الفيش في متابعة الطلبات.
- إن كان **يدوياً** أو بلا مزوّد: يبقى **قيد الانتظار** ليُنفّذه الأدمن (زر ✓).
- **أنواع المزوّدين** (قسم مزوّدي API):
  | النوع | الآلية | إعداد config |
  |------|--------|--------------|
  | بنك البينات (Havuz) | تسليم فوري لبين من مجموعة مرتبطة بالمزوّد | لا شيء — فقط اربط مجموعة بينات بالمزوّد |
  | نفس النظام (متجر داخلي) | يشتري فعلياً من متجر آخر على منصّتنا بحسابنا كوكيل لديه | `{"dealer_login": "رقم دخولنا هناك"}` + معرّف الباقة = **رقم منتج المورّد** |
  | متجر بطاقات (Barakat/Apstore) | GET `newOrder/{package_id}` بترويسة api-token | `{"code":"barakat","base_url":"...","api_token":"..."}` |
  | ZNET | GET `pin_ekle.php` بمعاملات kod/sifre | `{"code":"znet","base_url":"...","kod":"...","sifre":"..."}` |
  | منفّذ يدوي | لا تنفيذ آلي — يُتخطّى في السلسلة | — |

---

## 1) حسابات الاختبار

| الدور | الدخول | كلمة السر |
|------|--------|-----------|
| صاحب المتجر (علايا) | `5550000007` | `admin123` |
| وكيل (المشتري) | `bayi003` | `bayi123` |

بيئة الاختبار جاهزة مسبقاً في القاعدة:
- مزوّد **"بنك البينات الآلي"** (بينات AUTO-PIN-…)
- مزوّد **"بنك فارغ (للاختبار)"** (بلا بينات — لإجبار الفشل)
- متجر مورّد داخلي **supplier** فيه منتج وبينات SUPPLIER-PIN-…،
  ومزوّد **"متجر المورّد (داخلي)"** مربوط بحسابنا هناك (`alaya_at_supplier`).

---

## 2) السيناريوهات — ننفّذها معاً على الرابط الحيّ

> قبل البدء: ادخل كصاحب المتجر → **الألعاب → قائمة المنتجات** → لاحظ منتج
> **PUBG Mobile — 60 UC** (هو منتج التجارب). وافتح تبويباً آخر كوكيل `bayi003`.

### س1 — بنك البينات (بطاقات الشحن) ✅ مُختبر آلياً
1. على منتج 60 UC: الرئيسي = **بنك البينات الآلي** · API 1/2 = بديل مغلق.
2. كوكيل: البيع → PUBG → اشترِ 60 UC.
3. **المتوقع:** نافذة الشراء تعرض PIN فوراً (AUTO-PIN-…)، والطلب "ناجح" في متابعة الطلبات.

### س2 — التوجيه البديل (فشل → API 1) ✅ مُختبر آلياً
1. الرئيسي = **بنك فارغ (للاختبار)** · API 1 = **بنك البينات الآلي**.
2. اشترِ كوكيل.
3. **المتوقع:** ناجح مع PIN، وسجلّ الطلب يبدأ بـ
   `[بديل بعد: بنك فارغ …: لا يوجد بين متاح]` — أي فشل الرئيسي وتدخّل البديل.

### س3 — فشل السلسلة كاملة → عالق ✅ مُختبر آلياً
1. الرئيسي = بنك فارغ · API 1 = بنك فارغ · API 2 = بديل مغلق.
2. اشترِ كوكيل.
3. **المتوقع:** الطلب **"عالق"** والسجل يسرد كل المحاولات. (المبلغ مخصوم —
   الأدمن يقرّر: تنفيذ يدوي ✓ أو إلغاء ✗ فيسترجع المبلغ.)

### س4 — متجر داخلي (مستأجر ← مستأجر) ✅ مُختبر آلياً
1. الرئيسي = **متجر المورّد (داخلي)** · معرّف الباقة = رقم منتج المورّد
   (مضبوط مسبقاً في بيئة الاختبار).
2. اشترِ كوكيل.
3. **المتوقع:** ناجح مع PIN يبدأ بـ SUPPLIER-PIN، والسجل:
   `نُفّذ عبر متجر متجر المورّد · ref=tenant:supplier:…` —
   وفي متجر المورّد يظهر طلب مبيعات باسم حسابنا وخُصم رصيدنا لديه.

### س5 — التنفيذ اليدوي
1. اجعل تنفيذ منتجٍ ما "يدوي" (صفحة تفاصيل اللعبة) أو أزل مزوّديه.
2. اشترِ كوكيل → **قيد الانتظار** → كأدمن في متابعة الطلبات اضغط ✓ (تنفيذ) أو ✗ (إلغاء/استرجاع).

### س6 — مزوّد خارجي حقيقي (ZNET / Barakat) ⏳ ينتظر بياناتك
> هذا ما سنجرّبه معاً الآن — أرسل لي:
1. **نوع المزوّد**: znet أم barakat/apstore؟
2. **بيانات الاتصال**:
   - ZNET → `base_url` + `kod` (رقم الجوال) + `sifre` (كلمة السر)
   - Barakat → `base_url` + `api_token`
3. **معرّف باقة واحدة** لدى المزوّد (oyun id لـ ZNET / package id لـ Barakat)
   نربطها بمنتج 60 UC للتجربة.
4. أضيف المزوّد وأربطه، ثم تشتري كوكيل ونراقب معاً: الطلب يصل للمزوّد فعلاً،
   الحالة (ناجح/قيد التنفيذ/عالق)، والسجل الكامل.

---

## 3) أين تراقب النتائج

- **كوكيل:** طلباتي → عمود الكود/PIN + الحالة.
- **كأدمن:** الألعاب → متابعة الطلبات → اضغط **رقم الفيش** لعرض السجل الكامل
  (المحاولات، ref المزوّد، الرصيد قبل/بعد، رد النظام).
- **مزوّدو API:** أرصدة المزوّدين وأنواعهم.

## 4) ملاحظات مهمة (سلوك مقصود)

- الفشل الكامل **لا يسترجع المبلغ آلياً** — القرار للأدمن (إلغاء = استرجاع فوري).
- التوجيه الداخلي محميّ من الحلقات (متجر→متجر→متجر…) بعمق أقصاه 2.
- "قيد التنفيذ" (ZNET يؤكّد الاستلام ثم يجهّز الكود) توقف السلسلة — لا يُجرَّب
  البديل بعدها لأن الطلب وصل مزوّداً فعلاً.
- عند نجاح التوجيه الداخلي: التكلفة الفعلية = سعرنا لدى المورّد (يُسجّل في ref).

## 5) خلل اكتُشف وأُصلح أثناء هذا الفحص 🔧

محوّل بنك البينات كان يقرأ المزوّد من "رئيسي المنتج" لا من "المزوّد الجاري
تجريبه"، فكان البديل يبحث في بنك الرئيسي الفارغ. وُحّد توقيع المحوّلات
وصار كلٌّ يستلم مزوّده صراحةً — والسيناريوهات الأربعة خضراء.
