# plan2 — ما تبقّى

> **جرد كامل للمشروع، 2026-08-16.** مقروءٌ من الكود لا من الذاكرة.
> [plan.md](plan.md) سجلّ ما أُنجز جلسةً جلسة — وهذا الملف **ما لم يُنجَز**،
> مرتّباً بما يكلّفك إن تُرك.
>
> القاعدة في الترتيب: **ما يُفقد مالاً أو بيانات أولاً**، ثم ما يكسر ثقة
> المستخدم، ثم ما يُتعب، ثم ما يُجمّل.

---

## ✅ ما صار جاهزاً (كي لا يُعاد بناؤه)

النظام يعمل على <https://wtn.onrender.com> بأربعة أدوار ولوحاتها، والكتالوج
والتسعير والمحافظ والدفتر والطلبات والتقارير والتذاكر والفواتير والجرد.
و**137 نقطة نهاية** و**134 اختباراً خضراء**.

وأُنجز في الجلستين الأخيرتين:

- **إذن التوجيه الداخلي** — ثغرة كان بها أيّ متجرٍ ينفق رصيد وكيلٍ ليس له.
- **الواجهة الخارجية** بشكل ZDK + صفحة الربط في لوحة الوكيل + إذن صاحب المتجر.
- **متابعة المزوّد الداخلي** وتصحيح تكلفته وعملتها + حماية الخسارة قبل الطلب الأوّل.

---

## 🔴 المستوى ٠ — يُفقد مالاً أو بيانات

### ١. لا نسخة احتياطية للقاعدة
**ضاعت البيانات كلّها مرّةً فعلاً** في 2026-08-12 حين حُذفت قاعدة Render
المجانية ([info.md §1.5](info.md)). انتقلنا إلى Neon فزال سبب الحذف، **ولم
يزل الخطر**: لا يوجد في المستودع أي `pg_dump` ولا استعادة ولا جدولة — بحثتُ.

خطأٌ في ترحيل، أو حذفُ صفٍّ بالخطأ، أو حسابُ Neon يُغلق — ولا شيء يعود.

**ما يلزم:** أمرُ تصدير مجدول يومياً إلى مخزنٍ خارج Neon، و**تجربةُ استعادة
واحدة** تُثبت أن الملف يُقرأ فعلاً (نسخةٌ لم تُجرَّب ليست نسخة). وتحقّق أولاً
مما تعطيه خطة Neon من استرجاع زمني — قد يغطّي جزءاً فيقلّ ما تبنيه.

> **هذا أهمّ بند في الملف. كل ما تحته يُعوَّض، وهذا لا يُعوَّض.**

### ٢. لا عامل خلفي — متابعة الطلبات رهينة متصفّحٍ مفتوح
`sync_pending` لا يشغّلها إلا **لوحة الأدمن المفتوحة** (نبضة كل ٦ ثوانٍ من
المتصفّح). لا أمر إدارة ولا مجدول — تحقّقتُ.

فإن أغلق صاحب المتجر لوحته: الطلب الذي أكّد المزوّد استلامه يبقى «قيد التنفيذ»
**إلى الأبد**، ووكيله ينتظر، ومالُه محجوز.

وقد صار هذا **أثقل من قبل** بعد التوجيه الداخلي: موافقة المورّد اليدوية لا تصل
إلى المشتري إلا بنبضة، والمشتري متجرٌ آخر قد لا تكون لوحته مفتوحة أصلاً.

**ما يلزم:** أمر إدارة `sync_pending` لكل المتاجر + **Render Cron** كل دقيقة،
وقفلٌ على الطلب كي لا يعالجه اثنان معاً. (دَينٌ مفتوح منذ [plan.md §5](plan.md)
البند ١ — وهو أقدم دَين في المشروع.)

### ٣. التسعير الخاسر — عائق ٧
تكلفة `60 UC` لدى ZNET **43.40** وتُباع للوكيل بـ **32.00**: خسارة ≈ 11.40 على
كل طلب ناجح ([package-path.md §4](package-path.md)). وتوجيه إسلام إلى علايا كان
يخسر ~31$ في الطلب.

النظام صار يمسك هذا بنفسه (حماية الخسارة تحجب قبل الإرسال)، **لكن الحجب ليس
حلاً** — الطلب يصير «عالقاً» والبيع متوقّف. الحلّ مراجعة تسعيرية منك: تكلفة كل
باقة وسعرها لكل مجموعة.

---

## 🟠 المستوى ١ — أمان

### ٤. لا قفل بعد محاولات الدخول الفاشلة
`failed_login_count` يُعدّ ويُصفَّر… **ولا يُقرأ أبداً**. أي أن تجربة كلمات
السرّ آلياً مفتوحة بلا حدّ، على حساباتٍ تملك محافظ ومالاً.

**ما يلزم:** حدّ محاولات لكل رقم دخول (و/أو لكل IP) مع تهدئة تصاعدية، ورسالة
لا تكشف أيّ الحقلين خطأ.

### ٥. ZNET على `http://` غير مشفّر
`kod` و`sifre` يسافران في **الرابط** عبر اتصال غير مشفّر — يقرؤهما كل وسيط
على الطريق. تحقّق إن كان المزوّد يدعم `https` وبدّل `base_url`.

### ٦. سرّان على جهاز واحد بلا نسخة
`BOT_ENCRYPTION_KEY` إن ضاع ⇒ **تُفقد كل جلسات واتساب** ويلزم مسح QR من جديد
([info.md §5](info.md)). احتفظ بنسخة خارج الجهاز.

---

## 🟡 المستوى ٢ — نواقص يراها المستخدم

### ٧. الصفحة الرئيسية لصاحب المتجر فارغة
[Home.tsx](frontend/src/pages/Home.tsx) صندوقٌ مكتوب فيه «سنضيف هنا لاحقاً
ملخّص المبيعات والأرباح وأحدث الطلبات». وهي **أول ما يراه صاحب المتجر بعد كل
تسجيل دخول** — بينما لوحة الوكيل لها رئيسية بأربع بطاقات وإعلانات.

كل الأرقام موجودة في التقارير أصلاً؛ ما ينقص جمعُها في شاشة: مبيعات اليوم ·
الربح · طلبات عالقة تحتاج تدخّلك · وكلاء برصيد سالب · إيداعات تنتظر قرارك.

### ٨. صفحة إعدادات SMS لا ترسل شيئاً
هناك صفحة كاملة (مزوّد · مفتاح · مرسِل · تفعيل) وحقولٌ في القاعدة وقالبُ رسالة
على كل لعبة — و**لا سطرَ إرسالٍ واحد في المشروع كلّه**. بحثتُ: لا استدعاء
شبكة إلا في محوّلَي ZNET وZDK.

فصاحب المتجر يضبط الإعدادات ويظنّ الرسائل تُرسل. **إمّا تُبنى وإمّا تُخفى
الصفحة** — الوضع الحالي أسوأ من الاثنين.

### ٩. الاشتراك ينتهي ولا يترتّب عليه شيء
`sub_expires_at` يُحفظ ويُعرض و«يبقى X يوماً» تُحسب — **ولا شيء يفحصه** عند
الدخول أو الطلب. فمتجرٌ انتهى اشتراكه منذ شهر يعمل كما لو كان مشتركاً.

**قرارٌ لك قبل البناء:** ماذا يحدث عند الانتهاء؟ إيقافٌ كامل، أم منعُ الطلبات
الجديدة مع إبقاء القراءة، أم مهلة سماح ثم إيقاف؟

### ١٠. الوكيل لا يُعلَم بقرار إيداعه
يقبل صاحب المتجر إيداعاً أو يرفضه، ولا يصل الوكيل خبر — يراه إن فتح سجلّه.
(وكذلك موافقة التحصيل: نسجّلها ولا نُعلمه أن رسائل ستصله.)

### ١١. كوبونات الرصيد («إدخال كود الشحن»)
أجّلها المالك عمداً. تظهر في لوحات ZDK أعلى صفحة إضافة الرصيد.

---

## 🟢 المستوى ٣ — واتساب

البوت مبنيّ ويعمل، وبقي:

| # | البند |
|---|-------|
| ١٢ | **تجربة حيّة**: رقم ثانوي · مسح QR · رسالة فردية ثم جماعية صغيرة — لم يُختبر مع واتساب الحقيقي بعد |
| ١٣ | **تسخين الرقم** قبل أول إرسال جماعي — عليك لا على الكود، وهو أهمّ حاجز ضد الحظر |
| ١٤ | **الاستقبال** من مجموعات الجرد/الحسابات — المرحلة الثانية المؤجّلة |
| ١٥ | **البوت على جهازك** — ينام حين ينام الجهاز. سيرفر صغير مدفوع، ولا يتغيّر إلا `SITE_URL` |
| ١٦ | سجلّ إرسالٍ أوسع من آخر ١٠٠ رسالة |

---

## 🔵 المستوى ٤ — الواجهة الخارجية (جديدة)

### ١٧. أول عميل حقيقي
بُنيت واختُبرت (٣٩ اختباراً + دخانٌ حيّ) ولم يربط بها أحد بعد. راقب مع أوّلهم
ثلاثة أسئلة — تفصيلها في [OUR_API.md §8](docs/integrations/OUR_API.md):

1. **هل يحفظ `order_uuid` قبل الإرسال أم يولّده عند كل محاولة؟** إن فعل الثاني
   بطلت حماية الشحن المزدوج من طرفه — نمنع التكرار ولا نمنع معرّفاً جديداً.
2. هل يعالج `wait` بالاستعلام أم يعيد الإرسال؟
3. هل يقرأ `currency` أم يفترض عملة؟

### ١٨. Webhook بدل الاستعلام
العميل اليوم يسأل `check` دورياً. الدفع أنظف لكنه يحتاج طابوراً وإعادة محاولة
وتوقيعاً — ميزة قائمة بذاتها، لا تُبنى قبل أن يطلبها عميل.

---

## ⚪ المستوى ٥ — ديون وجودة

### ١٩. ستّ تطبيقات بلا اختبار واحد
| مُختبَر | بلا اختبار |
|---|---|
| catalog (618) · clientapi (398) · orders (373) · core (318) · providers (290) | **payments** (295 سطر عرض) · **whatsapp** (323) · **inventory** (323) · **agent** (377) · **superadmin** (222) · pool |

`payments` يمسّ المال مباشرةً (الإيداعات) و`inventory` يحسب الأرباح — وكلاهما
بلا شبكة أمان. وفي [plan.md §11](plan.md) ذكرٌ لـ**١٤٦ فحص دخان** كُتبت في
مجلّد مؤقّت ولم تُنقل إلى المستودع، فضاعت.

### ٢٠. رسالة خطأ ZDK المضلِّلة
`JSONDecodeError` يرث من `RequestException`، فيُعرض فشل التحليل كأنه فشل اتصال.
ضاع وقتٌ في تشخيصٍ خاطئ مرّةً. الفصل بينهما سطران.

### ٢١. ZDK لم يمرّ به طلب حقيقي
المحوّل مكتوب من الوثيقة كاملاً، ولم يُنفَّذ عبره طلب فعلي. الأسئلة المعلّقة في
[ZDK_API.md §6](docs/integrations/ZDK_API.md).

### ٢٢. حزمة الواجهة ٦٥٦ كيلوبايت في ملفّ واحد
البناء ينبّه في كل مرّة. على اتصال بطيء تأخيرٌ ملموس، وهو يُحمَّل كاملاً حتى
لدى وكيلٍ لا يفتح إلا صفحتين. التقسيم بالمسارات (`lazy`) يكفي.

### ٢٣. `tests.py` يحتاج قاعدة — والحلّ موجود فلا تنسه
```bash
python manage.py test --settings=config.settings_sqlite_test
```
بلا هذا تتجمّد الأوامر على مهلة اتصال Postgres غير المشغَّل محلياً.

### ٢٤. الخطة المجانية تنام
أول فتحٍ بعد الخمول يستغرق ~دقيقة. مقبولٌ للتجربة، **غير مقبول لعميل API خارجي**
يتوقّع ردّاً في ثوانٍ — نقطةٌ تصير حرجة مع البند ١٧.

---

## الترتيب الذي أوصي به

| # | لماذا الآن |
|---|---|
| **١. النسخ الاحتياطي** | الوحيد الذي لا يُعوَّض. وقد وقع مرّة |
| **٢. العامل الخلفي** | طلبات معلّقة = مال محجوز وثقة تُخسر. وصار ألزم بعد التوجيه الداخلي |
| **٣. التسعير** | كل طلب اليوم إمّا خاسر وإمّا محجوب |
| **٤. قفل الدخول** | ساعةٌ من العمل تسدّ باباً مفتوحاً على المحافظ |
| **٧. رئيسية صاحب المتجر** | أكثر شاشة تُفتح، وأرقامها جاهزة |
| **٨. SMS: ابنِ أو أخفِ** | صفحةٌ تكذب على مستعملها |
| **١٢. تجربة واتساب الحيّة** | مبنيٌّ ومدفوعٌ ثمنه ولم يُستعمل |

ثم الباقي بحسب ما يطلبه العميل.

---

## أشياء فحصتُها ووجدتُها سليمة

كي لا تُعاد مراجعتها:

- **عزل المتاجر** — كل استعلام مقيَّد بـ `tenant`؛ فحصتُ مسارات الوكلاء
  والطلبات والمنتجات والواجهة الخارجية.
- **`extra.price` بعملة الدفتر** في كل مواضع الكتابة الثلاثة (شككتُ فيه أمس
  وظننتُ الدلالة مختلطة — راجعتُ الكود فوجدتُه محوّلاً في كلّها).
- **الأسرار** خارج المستودع: `SECRET_KEY` يولّده Render · `DATABASE_URL`
  و`INTERNAL_API_KEY` بـ `sync: false` · `DEBUG=0` في الإنتاج.
- **الواجهة الداخلية للبوت** مغلقة 503 بلا `INTERNAL_API_KEY`.
- **الفرع** صار `main` على Render، والنشر التلقائي يعمل (تحقّقتُ من نزول
  البناء الأخير حيّاً).
