# plan2 — ما تبقّى

> **جرد كامل للمشروع، 2026-08-16** · حُدِّث بعد جلسة التنفيذ نفس اليوم. مقروءٌ من الكود لا من الذاكرة.
> [plan.md](plan.md) سجلّ ما أُنجز جلسةً جلسة — وهذا الملف **ما لم يُنجَز**،
> مرتّباً بما يكلّفك إن تُرك.
>
> القاعدة في الترتيب: **ما يُفقد مالاً أو بيانات أولاً**، ثم ما يكسر ثقة
> المستخدم، ثم ما يُتعب، ثم ما يُجمّل.

---

## ▶️ نبدأ من هنا غداً

**النطاقات الفرعية للمتاجر** — البند ٢٥ أسفل هذا الملف. كل ما عداه إمّا أُنجز
أو مؤجَّل بقرارك.

**وأنت تحتاج أن تجهّز شيئاً واحداً قبل أن نبدأ:** مفتاح API من Cloudflare.
`dash.cloudflare.com` ← **My Profile** ← **API Tokens** ← **Create Token** ←
قالب **Edit zone DNS** ← اختر `wtn4.com` ← أنشئه واحتفظ به.

---

## ✅ ما صار جاهزاً (كي لا يُعاد بناؤه)

النظام يعمل على **<https://wtn4.com>** (خادمنا على Hetzner) بأربعة أدوار ولوحاتها، والكتالوج
والتسعير والمحافظ والدفتر والطلبات والتقارير والتذاكر والفواتير والجرد.
و**١٤٤ نقطة نهاية** و**١٨٥ اختباراً خضراء**.

وأُنجز في الجلستين الأخيرتين:

- **إذن التوجيه الداخلي** — ثغرة كان بها أيّ متجرٍ ينفق رصيد وكيلٍ ليس له.
- **الواجهة الخارجية** بشكل ZDK + صفحة الربط في لوحة الوكيل + إذن صاحب المتجر.
- **متابعة المزوّد الداخلي** وتصحيح تكلفته وعملتها + حماية الخسارة قبل الطلب الأوّل.

---

## 🔴 المستوى ٠ — يُفقد مالاً أو بيانات

### ✅ ١. النسخ الاحتياطي — **أُنجز مع النقل إلى Hetzner (2026-08-16)**
نسخة يومية 03:17 إلى `/opt/wtn-backups`، تُحفظ ثلاثين يوماً، مع فحص حجمٍ يمسك
فشل `pg_dump` المقنّع. جُرّبت حيّاً: ٤٤ جدولاً وبيانات الطلبات الفعلية داخلها.
وتعليمات الاستعادة في آخر [deploy/backup.sh](deploy/backup.sh) — **ونسخةٌ لم
تُجرَّب استعادتها ليست نسخة**، فجرّبها مرّةً على قاعدة فارغة.

<details><summary>النصّ القديم للبند</summary>

### ~~١. لا نسخة احتياطية للقاعدة~~ — **مؤجّلة حتى الانتقال إلى Hetzner**
> القرار (2026-08-16): يُبنى النسخ الاحتياطي **بعد** نقل المشروع من Render إلى
> خادم Hetzner مدفوع، متى اكتملت التجربة. والخطر قائم إلى ذلك الحين — فلا
> تُدخل بيانات لا تحتمل ضياعها.

**ضاعت البيانات كلّها مرّةً فعلاً** في 2026-08-12 حين حُذفت قاعدة Render
المجانية ([info.md §1.5](info.md)). انتقلنا إلى Neon فزال سبب الحذف، **ولم
يزل الخطر**: لا يوجد في المستودع أي `pg_dump` ولا استعادة ولا جدولة — بحثتُ.

خطأٌ في ترحيل، أو حذفُ صفٍّ بالخطأ، أو حسابُ Neon يُغلق — ولا شيء يعود.

**ما يلزم:** أمرُ تصدير مجدول يومياً إلى مخزنٍ خارج Neon، و**تجربةُ استعادة
واحدة** تُثبت أن الملف يُقرأ فعلاً (نسخةٌ لم تُجرَّب ليست نسخة). وتحقّق أولاً
مما تعطيه خطة Neon من استرجاع زمني — قد يغطّي جزءاً فيقلّ ما تبنيه.

> **كان أهمّ بند في الملف. كل ما تحته يُعوَّض، وهذا لا يُعوَّض.**

</details>

### ✅ ٢. العامل الخلفي — **أُنجز (2026-08-16)**
أمر `sync_orders` يتابع الطلبات لكل المتاجر، تشغّله مهمّة مجدولة **كل دقيقة**
على الخادم. ويحمل قفل ملفّ: النبضة كل دقيقة والمتابعة قد تطول، ولو تراكبتا
لحُسم الطلب مرّتين — أي استرجاعان لمبلغ واحد.

<details><summary>النصّ القديم للبند</summary>

### ~~٢. لا عامل خلفي~~ — متابعة الطلبات رهينة متصفّحٍ مفتوح
`sync_pending` لا يشغّلها إلا **لوحة الأدمن المفتوحة** (نبضة كل ٦ ثوانٍ من
المتصفّح). لا أمر إدارة ولا مجدول — تحقّقتُ.

فإن أغلق صاحب المتجر لوحته: الطلب الذي أكّد المزوّد استلامه يبقى «قيد التنفيذ»
**إلى الأبد**، ووكيله ينتظر، ومالُه محجوز.

وقد صار هذا **أثقل من قبل** بعد التوجيه الداخلي: موافقة المورّد اليدوية لا تصل
إلى المشتري إلا بنبضة، والمشتري متجرٌ آخر قد لا تكون لوحته مفتوحة أصلاً.

**ما يلزم:** أمر إدارة `sync_pending` لكل المتاجر + **Render Cron** كل دقيقة،
وقفلٌ على الطلب كي لا يعالجه اثنان معاً. (دَينٌ مفتوح منذ [plan.md §5](plan.md)
البند ١ — وهو أقدم دَين في المشروع.)

</details>

### ٣. التسعير الخاسر — عائق ٧
تكلفة `60 UC` لدى ZNET **43.40** وتُباع للوكيل بـ **32.00**: خسارة ≈ 11.40 على
كل طلب ناجح ([package-path.md §4](package-path.md)). وتوجيه إسلام إلى علايا كان
يخسر ~31$ في الطلب.

النظام صار يمسك هذا بنفسه (حماية الخسارة تحجب قبل الإرسال)، **لكن الحجب ليس
حلاً** — الطلب يصير «عالقاً» والبيع متوقّف. الحلّ مراجعة تسعيرية منك: تكلفة كل
باقة وسعرها لكل مجموعة.

---

## 🟠 المستوى ١ — أمان

### ✅ ٤. قفل بعد محاولات الدخول الفاشلة — **أُنجز**
كان `failed_login_count` يُعدّ ويُصفَّر **ولا يُقرأ أبداً**. صار: ثلاث محاولات
ثم قفل، والقفل يُفحص **قبل** كلمة السرّ فلا يُختبر التخمين أصلاً. ومخرجه
الوحيد كلمةُ سرٍّ جديدة من صاحب المتجر — لأن القديمة ثبت أن أحدهم يخمّنها.
ولمالك المنصّة `manage.py unlock_user` إذ لا أحد فوقه.

### ٥. ZNET على `http://` غير مشفّر
`kod` و`sifre` يسافران في **الرابط** عبر اتصال غير مشفّر — يقرؤهما كل وسيط
على الطريق. تحقّق إن كان المزوّد يدعم `https` وبدّل `base_url`.

### ٦. سرّان على جهاز واحد بلا نسخة
`BOT_ENCRYPTION_KEY` إن ضاع ⇒ **تُفقد كل جلسات واتساب** ويلزم مسح QR من جديد
([info.md §5](info.md)). احتفظ بنسخة خارج الجهاز.

---

## 🟡 المستوى ٢ — نواقص يراها المستخدم

### ✅ ٧. الصفحتان الرئيسيتان — **أُنجزتا بالبطاقات**
نموذج `HomeCard` واحد لسلسلتين: **مالك المنصّة يكتب لأصحاب المتاجر**
(لوحة المنصّة ← بطاقات المتاجر · ويجوز توجيه بطاقة لمتجر بعينه)، و**صاحب
المتجر يكتب لوكلائه** (الإعدادات ← بطاقات الوكلاء). لونا خلفية بتدرّج ولون
خطّ وأيقونة ورابط، بمعاينة حيّة وترتيب وإخفاء.

> **ما لم يُبنَ:** بطاقاتُ أرقامٍ تلقائية في رئيسية صاحب المتجر (مبيعات اليوم ·
> طلبات عالقة · وكلاء برصيد سالب · إيداعات تنتظر قراره). الأرقام كلّها موجودة
> في التقارير، وجمعُها في شريطٍ أعلى البطاقات عملُ جلسة.

### ⏸ ٨. SMS — **مؤجّلة بقرار المالك**، والصفحة لم تعد تكذب
لا سطرَ إرسالٍ واحد في المشروع. البناء مؤجَّل، وأُضيفت لافتة صريحة على الصفحة:
«الخدمة غير مفعّلة بعد — ما تضبطه يُحفظ ولا تُرسل رسائل، واستعمل واتساب اليوم».
تُحذف اللافتة يوم يُبنى الإرسال.

### ✅ ٩. الاشتراك يُطبَّق — **أُنجز**
مهلة سماح ثم منع الشراء وحده (قرار المالك). أربع حالات: `ok` · `warn` (قبل
٧ أيام) · `grace` · `blocked`. البوّابة في `create_order` وحدها فتغطّي كل
مدخل — المتجر واللوحة والواجهة الخارجية والتوجيه الداخلي. ومالك المنصّة يضبط
أيام السماح وتاريخ الانتهاء ويُعفي متجراً من المنع أصلاً.

### ١٠. الوكيل لا يُعلَم بقرار إيداعه
يقبل صاحب المتجر إيداعاً أو يرفضه، ولا يصل الوكيل خبر — يراه إن فتح سجلّه.
(وكذلك موافقة التحصيل: نسجّلها ولا نُعلمه أن رسائل ستصله.)

### ✅ ١٠.٥ قائمة الباقات للوكيل — **أُنجزت**
قسمٌ داخل «البيع» (لا تبويباً تاسعاً): كل الباقات بأسعار شرائه هو وسعر التوصية،
و**رقم المنتج عندنا** بزرّ نسخ — وهو ما يضعه في `newOrder/{الرقم}/params`.
ورقمُ الباقة لدى مزوّد المتجر **لا يُعرض بحال**: سرٌّ تجاري يدلّ الوكلاء على
مصادر صاحب المتجر، ويحرسه اختبار.

### ١١. كوبونات الرصيد («إدخال كود الشحن»)
أجّلها المالك عمداً. تظهر في لوحات ZDK أعلى صفحة إضافة الرصيد.

---

## 🟢 المستوى ٣ — واتساب

البوت مبنيّ ويعمل، وبقي:

| # | البند |
|---|-------|
| ١٢ | **تجربة حيّة**: رقم ثانوي · مسح QR · رسالة فردية ثم جماعية صغيرة — لم يُختبر مع واتساب الحقيقي بعد |
| ١٣ | **تسخين الرقم** قبل أول إرسال جماعي — عليك لا على الكود، وهو أهمّ حاجز ضد الحظر |
| ١٤ | **الاستقبال** من مجموعات الجرد/الحسابات — المرحلة الثانية المؤجّلة |
| ~~١٥~~ | ✅ **البوت انتقل إلى الخادم** (2026-08-16) — لم يعد ينام مع جهازك |
| ١٦ | سجلّ إرسالٍ أوسع من آخر ١٠٠ رسالة |

---

## 🔵 المستوى ٤ — الواجهة الخارجية (جديدة)

### ١٧. أول عميل حقيقي
بُنيت واختُبرت (٣٩ اختباراً + دخانٌ حيّ) ولم يربط بها أحد بعد. راقب مع أوّلهم
ثلاثة أسئلة — تفصيلها في [OUR_API.md §8](docs/integrations/OUR_API.md):

1. **هل يحفظ `order_uuid` قبل الإرسال أم يولّده عند كل محاولة؟** إن فعل الثاني
   بطلت حماية الشحن المزدوج من طرفه — نمنع التكرار ولا نمنع معرّفاً جديداً.
2. هل يعالج `wait` بالاستعلام أم يعيد الإرسال؟
3. هل يقرأ `currency` أم يفترض عملة؟

### ١٨. Webhook بدل الاستعلام
العميل اليوم يسأل `check` دورياً. الدفع أنظف لكنه يحتاج طابوراً وإعادة محاولة
وتوقيعاً — ميزة قائمة بذاتها، لا تُبنى قبل أن يطلبها عميل.

---

## ⚪ المستوى ٥ — ديون وجودة

### ١٩. ستّ تطبيقات بلا اختبار واحد
| مُختبَر | بلا اختبار |
|---|---|
| catalog (618) · clientapi (398) · orders (373) · core (318) · providers (290) | **payments** (295 سطر عرض) · **whatsapp** (323) · **inventory** (323) · **agent** (377) · **superadmin** (222) · pool |

`payments` يمسّ المال مباشرةً (الإيداعات) و`inventory` يحسب الأرباح — وكلاهما
بلا شبكة أمان. وفي [plan.md §11](plan.md) ذكرٌ لـ**١٤٦ فحص دخان** كُتبت في
مجلّد مؤقّت ولم تُنقل إلى المستودع، فضاعت.

### ٢٠. رسالة خطأ ZDK المضلِّلة
`JSONDecodeError` يرث من `RequestException`، فيُعرض فشل التحليل كأنه فشل اتصال.
ضاع وقتٌ في تشخيصٍ خاطئ مرّةً. الفصل بينهما سطران.

### ٢١. ZDK لم يمرّ به طلب حقيقي
المحوّل مكتوب من الوثيقة كاملاً، ولم يُنفَّذ عبره طلب فعلي. الأسئلة المعلّقة في
[ZDK_API.md §6](docs/integrations/ZDK_API.md).

### ٢٢. حزمة الواجهة ٦٥٦ كيلوبايت في ملفّ واحد
البناء ينبّه في كل مرّة. على اتصال بطيء تأخيرٌ ملموس، وهو يُحمَّل كاملاً حتى
لدى وكيلٍ لا يفتح إلا صفحتين. التقسيم بالمسارات (`lazy`) يكفي.

### ٢٣. `tests.py` يحتاج قاعدة — والحلّ موجود فلا تنسه
```bash
python manage.py test --settings=config.settings_sqlite_test
```
بلا هذا تتجمّد الأوامر على مهلة اتصال Postgres غير المشغَّل محلياً.

### ✅ ٢٤. الخمول — **زال بالنقل** (2026-08-16)
الخادم لا ينام. وكانت الخطة المجانية تستغرق ~دقيقة لأول فتح — وهو ما لا
يحتمله عميل API خارجي.

---

## 🟣 المستوى ٦ — النطاقات الفرعية (التالي)

### ٢٥. لكل متجر عنوانه: `islam.wtn4.com`

**الحال اليوم:** كل المتاجر تدخل من `wtn4.com`، وكلٌّ يجد لوحته **بحسابه**.
والعنوان المعروض في لوحة المنصّة عنوانٌ **مُزمَع** لا عامل — ضغطُه يعطي
`SSL_ERROR_INTERNAL_ERROR_ALERT` لأن لا شهادة له. ولذلك يُعرض رمادياً ومكتوبٌ
تحته «غير مفعّل بعد».

**لماذا نبنيه:** العنوان المستقلّ ورقة بيع حقيقية لصاحب المتجر — يعطيه لوكلائه
بشعاره ولونه. وهو ما تفعله المنصّات المنافسة.

#### الجاهز فعلاً

| | |
|---|---|
| `*.wtn4.com` في Cloudflare | ✅ موجود من قبلنا |
| قالب الشهادة الشاملة | ✅ مكتوبٌ **معطَّلاً** في [deploy/Caddyfile](deploy/Caddyfile) |
| شعار كل متجر ولونه وخطّه | ✅ في `Tenant` (`theme_config` · `logo_url`) |
| `Tenant.subdomain` بتحقّق من الشكل | ✅ أُضيف مع تعديل المتاجر |

#### الناقص

1. **الشهادة الشاملة** — تحتاج تحقّقاً عبر DNS بمفتاح Cloudflare (تُنشئه أنت،
   انظر رأس الملف). ثم يُفعَّل القالب المعطَّل في `Caddyfile`.
2. **وسيطٌ يستنتج المتجر من العنوان** — يقرأ `Host`، يقتطع الجزء الأول، ويضع
   المتجر على الطلب. وهو القطعة الوحيدة التي تحتاج تفكيراً:
   - `wtn4.com` و`www` و`api` **ليست متاجر** — تبقى الباب العام ولوحة المنصّة.
   - نطاقٌ لا يقابله متجر ⇒ صفحة «لا متجر بهذا العنوان»، لا صفحة الدخول العامة
     (وإلّا ظنّ الزائر أن العنوان صحيح).
   - متجرٌ موقوف أو انتهى اشتراكه ⇒ ما الذي يظهر؟ **قرارٌ لك.**
3. **صفحة دخولٍ للمتجر** — شعاره ولونه واسمه. البيانات موجودة، ينقص وصلها.
4. **منع الخلط** — وكيل علايا لا يدخل من `islam.wtn4.com`. `login_id` فريد عبر
   المنصّة كلّها، فالدخول ينجح اليوم من أي عنوان؛ يجب أن يُرفض بعد الوسيط.
5. **`ALLOWED_HOSTS`** — يصير `.wtn4.com` (نقطةٌ بادئة تعني «وما تحته»).
6. **السحابة البرتقالية في Cloudflare** — تُشعَل بعد أن تعمل الشهادة، بوضع
   `Full (strict)` لا `Flexible` (وإلّا حلقةُ تحويلٍ لا تنتهي).

> **ملاحظة على الترتيب:** أُجّل هذا عمداً حتى يستقرّ النقل. النقل يجب أن يمرّ
> بلا مفاجآت، ولو خُلط بميزةٍ جديدة وحدث عطلٌ لما عُرف أمن النقل أم من الجديد.
> وقد استقرّ النقل الآن.

**التقدير:** يوم عمل. ولا يكسر شيئاً — `wtn4.com` يبقى يعمل كما هو.

---

## الترتيب الذي أوصي به

| # | لماذا الآن |
|---|---|
| **١. النسخ الاحتياطي** | الوحيد الذي لا يُعوَّض. وقد وقع مرّة |
| **٢. العامل الخلفي** | طلبات معلّقة = مال محجوز وثقة تُخسر. وصار ألزم بعد التوجيه الداخلي |
| **٣. التسعير** | كل طلب اليوم إمّا خاسر وإمّا محجوب |
| **٢٥. النطاقات الفرعية** | التالي — عنوانٌ لكل متجر، وورقة بيعٍ لك |
| **٣. التسعير** | كل طلبٍ اليوم إمّا خاسر وإمّا محجوب |
| **١٢. تجربة واتساب الحيّة** | مبنيٌّ ومدفوعٌ ثمنه ولم يُستعمل |
| **٥. ZNET على http** | اعتمادُك مكشوفٌ في الرابط لكل وسيط |

**أُنجزت 2026-08-16:** ~~١ النسخ الاحتياطي~~ · ~~٢ العامل الخلفي~~ ·
~~٤ قفل الدخول~~ · ~~٧ الرئيسية~~ · ~~٩ الاشتراك~~ · ~~١٠.٥ قائمة الباقات~~ ·
~~١٥ البوت~~ · ~~٢٤ الخمول~~ · وتعديلُ المتاجر وحذفُها · وعملةُ كل مزوّد.

ثم الباقي بحسب ما يطلبه العميل.

---

## أشياء فحصتُها ووجدتُها سليمة

كي لا تُعاد مراجعتها:

- **عزل المتاجر** — كل استعلام مقيَّد بـ `tenant`؛ فحصتُ مسارات الوكلاء
  والطلبات والمنتجات والواجهة الخارجية.
- **`extra.price` بعملة الدفتر** في كل مواضع الكتابة الثلاثة (شككتُ فيه أمس
  وظننتُ الدلالة مختلطة — راجعتُ الكود فوجدتُه محوّلاً في كلّها).
- **الأسرار** خارج المستودع: `SECRET_KEY` يولّده Render · `DATABASE_URL`
  و`INTERNAL_API_KEY` بـ `sync: false` · `DEBUG=0` في الإنتاج.
- **الواجهة الداخلية للبوت** مغلقة 503 بلا `INTERNAL_API_KEY`.
- **الفرع** صار `main` على Render، والنشر التلقائي يعمل (تحقّقتُ من نزول
  البناء الأخير حيّاً).
