# info — معلومات النشر والوصول

> ملف مرجعي سريع: **أين الموقع مرفوع** · **كيف نرفعه ونحدّثه** · **حسابات الاختبار**.

---

## 0. سياسة الفروع — `main` وحده 🚫🌿

**قرار المالك (2026-08-05):** لا نعمل على فروع أخرى إطلاقاً. **كل التطوير والالتزام
والرفع يجري مباشرةً على `main`.**

- ❌ لا تُنشأ فروع ميزات (`feature/…`) ولا فروع وكلاء (`claude/…`) ولا Pull Requests.
- ✅ أي فرع قائم يُدمَج فوراً في `main` ثم يُحذف.
- ✅ التسلسل المعتاد: `git add -A` ← `git commit` ← `git push origin main`.
- ✅ **إعداد Render:** فرع خدمة `wtn` صار `main` (بُدِّل 2026-08-15).

> **ما كلّفه هذا:** بقي الفرع على `claude/website-clone-auth-access-xp4oj8`
> أياماً بعد الدمج، فتراكمت **خمسة التزامات** على `main` والموقع يعمل بكودٍ
> قديم بينما نظنّه محدَّثاً. الدرس: بعد أي دفعة، تأكّد أن الأثر ظهر على الموقع
> لا أن الدفع نجح.

> **الحالة (2026-08-05):** فرع `claude/website-clone-auth-access-xp4oj8` دُمج في `main`
> بـ fast-forward (113 التزاماً)، والفرعان الآن على نفس الالتزام. يُحذف الفرع القديم
> فور تبديل إعداد الفرع على Render إلى `main`.

---

## 1. أين الموقع مرفوع

> **انتقل الموقع إلى خادمنا (2026-08-16).** التفاصيل الكاملة في
> [deploy/README.md](deploy/README.md). وRender يبقى يعمل مؤقّتاً على **نفس
> القاعدة** حتى نطمئنّ، ثم يُطفأ.

| البند | القيمة |
|------|--------|
| **الاستضافة** | خادم **Hetzner CX23** · Falkenstein · Ubuntu 24.04 |
| **الرابط المباشر** | <https://wtn4.com> (و`www` و`api`) |
| **عنوان الخادم** | `46.224.47.213` — الدخول بمفتاح SSH، ولا كلمة سرّ |
| **النطاق** | `wtn4.com` عبر Cloudflare — **`DNS only`** أثناء الإعداد |
| **المستودع** | `https://github.com/Lebid15/wtn` |
| **الفرع المنشور** | `main` — **الفرع الوحيد المعتمد** (انظر §0) |
| **قاعدة البيانات** | **[Neon](https://neon.tech)** — خارج الخادم عمداً (انظر §1.5) |
| **ملفات النشر** | [deploy/](deploy/) — في المستودع لا مكتوبةً يدوياً على الخادم |
| **الاستضافة السابقة** | Render (خطة Free) — <https://wtn.onrender.com> · تُطفأ بعد التأكّد |

**ما ربحناه بالنقل** — ثلاثة بنود من [plan2.md](plan2.md) أُغلقت دفعةً واحدة:

| | |
|---|---|
| **العامل الخلفي** (أقدم دَين) | مهمّة كل دقيقة تتابع الطلبات، فلا يتجمّد طلبٌ لأن أحداً أغلق اللوحة |
| **بوت واتساب** | صار على الخادم، فلا ينام مع جهاز المالك |
| **الخادم لا ينام** | كانت الخطة المجانية تنام بعد ١٥ دقيقة — وهو ما لا يحتمله عميل API |
| **النسخ الاحتياطي** | نسخة يومية 03:17 إلى `/opt/wtn-backups` — أُجّلت حتى هذه اللحظة بالضبط |

**التشغيل اليومي:**
```bash
ssh root@46.224.47.213
cd /opt/wtn && git pull && docker compose -f deploy/docker-compose.yml up -d --build
```

**البنية على الخادم:** ثلاث حاويات — `web` (Django يقدّم الـ API *و* واجهة
React من نفس الأصل، فلا حاجة إلى CORS)، و`caddy` موجّهاً يصنع شهادة HTTPS
ويجدّدها وحده، و`bot` لواتساب.

**البوت ينادي الموقع داخلياً** (`http://web:8000`) لا عبر الإنترنت: مناداةُ
الخادم عنوانَه العام من داخله تفشل على أكثر المستضيفات. ولذلك `web` مذكورٌ في
`ALLOWED_HOSTS` — بدونه يردّ Django بـ 400 برسالةٍ لا تقول أن الاسم هو السبب.

---

## 1.5 قاعدة البيانات — خارج Render 🗄️

**ما حدث (2026-08-12):** توقّف الموقع تماماً. السبب أن قاعدة `wtn-db` المجانية
على Render **حُذفت نهائياً**: الخطة المجانية تعمر **٣٠ يوماً** ثم تُمحى (كانت
أُنشئت 2026-07-11). فصار `migrate` يفشل بـ
`failed to resolve host 'dpg-…': Name or service not known`، و`set -e` في
[backend/start.sh](backend/start.sh) يقتل الحاوية، فلا يُشغَّل `gunicorn` أصلاً.

> ⚠️ **ضاعت كل البيانات** لأنه لم تكن هناك أي نسخة احتياطية. أخذُ نسخ احتياطية
> ما زال **غير منجَز** — انظر [plan.md](plan.md).

**الحلّ الدائم:** القاعدة انتقلت إلى **Neon** — مزوّد PostgreSQL مُدار، خطته
المجانية **لا تنتهي ولا تُحذف**. وهي خارج Render تماماً، فلا يمسّها إعادة نشرٍ
ولا حذف خدمة.

| البند | القيمة |
|------|--------|
| **المزوّد** | <https://neon.tech> — خطة Free دائمة |
| **الوصل** | متغيّر بيئة واحد `DATABASE_URL` في لوحة Render |
| **الصيغة** | `postgresql://user:pass@ep-xxx.region.aws.neon.tech/wtn_db?sslmode=require` |
| **التشفير** | `sslmode=require` — يُفرض تلقائياً في [settings.py](backend/config/settings.py) حتى لو غاب من الرابط |

**كيف يقرأه الكود:** في [backend/config/settings.py](backend/config/settings.py)
يسبق `DATABASE_URL` كلَّ ما عداه؛ فإن غاب رجع الكود إلى متغيّرات `DB_*`
المنفصلة (وهي ما تستخدمه النسخة المحلية). فأي بقايا `DB_HOST` قديمة في لوحة
Render لا تضرّ — تُتجاهَل ما دام الرابط موجوداً.

### تبديل القاعدة لاحقاً
لوحة Render ← خدمة `wtn` ← **Environment** ← عدّل `DATABASE_URL` ← احفظ.
يعيد Render النشر تلقائياً، و`start.sh` يشغّل `migrate` والبذور على القاعدة
الجديدة. لا يلزم تعديل كود ولا إعادة بناء.

---

## 2. طريقة الرفع (أول مرة — تمّت بالفعل)

الملفات الجاهزة في المستودع: [Dockerfile](Dockerfile) · [render.yaml](render.yaml) ·
[backend/start.sh](backend/start.sh) · [docs/DEPLOY.md](docs/DEPLOY.md).

1. الدخول إلى <https://render.com> والتسجيل بحساب **GitHub** (مجاني).
2. من اللوحة: **New → Blueprint**.
3. اختيار مستودع `Lebid15/wtn` والفرع `main` (§0).
4. الضغط على **Apply** — يقرأ Render ملف `render.yaml` وينشئ خدمة الويب `wtn`
   (Docker · free) — تبني الواجهة ثم الخلفية. **لا ينشئ قاعدة بيانات** — فهي
   على Neon خارج Render (§1.5).
5. لصق `DATABASE_URL` في **Environment** — بدونه لا يقلع الموقع.
6. أول إقلاع يشغّل الترحيلات (`migrate`) ويزرع البيانات التجريبية.
7. بعد ~3–5 دقائق يصبح الرابط `https://wtn.onrender.com` جاهزاً.

### متغيّرات البيئة
كلها داخل `render.yaml` عدا `DATABASE_URL` — وهو **الوحيد** الذي يُضبط يدوياً:

| المتغيّر | المصدر |
|---------|--------|
| `SECRET_KEY` | يولّده Render تلقائياً (`generateValue`) |
| `DEBUG` | `"0"` |
| `ALLOWED_HOSTS` | `"*"` |
| `INTERNAL_API_KEY` | يولّده Render تلقائياً — السرّ المشترك مع بوت واتساب (§5) |
| `DATABASE_URL` | **يدوي مرّةً واحدة** (`sync: false`) — رابط Neon، يُلصق من لوحة Render (§1.5) |

---

## 3. طريقة التحديث (كل مرة لاحقاً)

النشر **تلقائي**: أي دفعة إلى `main` تُطلق بناءً جديداً على Render (§0 — لا فروع أخرى).

```bash
git add -A
git commit -m "وصف التغيير"
git push origin main
```

ثم تُتابَع حالة البناء من Render Dashboard → خدمة `wtn` → **Logs**
(البناء ~3–5 دقائق). يمكن أيضاً إعادة النشر يدوياً من **Manual Deploy**.

### ماذا يحدث في كل نشر
1. **Docker مرحلة 1:** `npm ci` ثم `npm run build` للواجهة (Vite) —
   ملف `.env.production` يضبط `VITE_API_URL=/api`.
2. **Docker مرحلة 2:** تثبيت `backend/requirements.txt` + نسخ `frontend/dist`
   إلى `/app/frontend_dist` (يقدّمها WhiteNoise مع SPA fallback) + `collectstatic`.
3. **`backend/start.sh`:** `migrate` → بذور متسامحة (`seed_demo` · `seed_library` ·
   `seed_auto` · `seed_payment_methods` تعمل في كل نشر · بذور الكتالوج الثقيلة
   مرّة واحدة فقط عند قاعدة فارغة) → تشغيل `gunicorn` بعاملين.

---

## 4. حسابات الاختبار

| الدور | اسم الدخول | كلمة السر | لوحته |
|------|-----------|-----------|-------|
| مالك المنصّة (`platform_owner`) | `9990000000` | `super123` | `/platform` |
| صاحب المتجر (`tenant_admin`) | `5550000007` | `admin123` | لوحة الإدارة (`/dealers` …) |
| وكيل كبير (`ana_bayi`) | `5552222222` | `big123` | `/bigagent` |
| وكيل (`bayi`) | `bayi003` | `bayi123` | `/store` |

> العميل النهائي بلا حساب. تفاصيل الأدوار في [docs/ROLES.md](docs/ROLES.md).

---

## 4.5 العملات

- **عملة الدفتر** واحدة لكل متجر (`Tenant.base_currency`، الافتراضي للمتاجر
  الجديدة `USD`). كل رقم في قاعدة البيانات محفوظ بها.
- **عملة عرض الوكيل** (`User.display_currency`) عرضٌ فقط: يضبطها صاحب المتجر من
  **الإعدادات ← أسعار الوكلاء** لكل وكيل ووكيل كبير، فيرى صاحبها رصيده وأسعار
  الباقات وطلباته وتقاريره بعملته. لا تُخزَّن أي قيمة بها، ولوحة صاحب المتجر
  ومالك المنصّة تبقيان بعملة الدفتر.
- **أسعار الصرف** من **الإعدادات ← أسعار الصرف** — مرجع واحد للموقع كلّه.
  الاصطلاح: `exchange_rates[CUR]` = **كم وحدةً من CUR تساوي وحدةً من عملة
  الموقع** — أي مع دفترٍ بالدولار `{"TRY": "41.5"}` تعني «1$ = 41.5 ل.ت».
  وهو نفس الرقم الذي يكتبه المالك، لا مقلوبه.

> **تمّ التحويل إلى الدولار:** ترحيل `core.0015_ledger_to_usd` حوّل دفتر كل متجر
> من الليرة التركية إلى الدولار مرّةً واحدة عند النشر (بسعر الدولار المحفوظ في
> «أسعار الصرف»، أو 41.5 إن لم يكن مضبوطاً). يسجّله Django فلا يتكرّر، ولا يمسّ
> متجراً عملته دولار أصلاً.

### تحويل عملة دفتر متجر قائم
أمر يدوي لا يُشغَّل في النشر — لأي تحويل لاحق. يبدأ بالمعاينة، ثم `--apply`
للتنفيذ داخل معاملة واحدة:

```bash
python manage.py convert_base_currency --to USD --rate 41.5 --tenant alaya
python manage.py convert_base_currency --to USD --rate 41.5 --tenant alaya --apply
```

`--rate` = كم وحدةً من العملة الحالية تساوي وحدةً من الجديدة. لا تُمسّ أسعار
اشتراك المنصّة ولا فواتيرها (دفتر بين مالك المنصّة والمتجر، لا دفتر المتجر).

---

## 5. واتساب — البوت يعمل خارج Render

الموقع **لا يتّصل بواتساب**. هو يكتب الرسائل في طابور بقاعدة البيانات، وبرنامج
منفصل ([`bot/`](bot/)) يسأله كل ثلاث ثوانٍ فيرسل ويعيد النتيجة.

**لماذا خارج Render:** الخطة المجانية تنام بعد ~15 دقيقة، وواتساب يحتاج اتصالاً
دائماً. وتَكرارُ القطع والوصل أوضح بصمة آلية يعاقب عليها واتساب بحظر الرقم.

| البند | القيمة |
|------|--------|
| **أين يعمل الآن** | جهاز المالك — `cd bot && npm start` |
| **لاحقاً** | سيرفر صغير مدفوع؛ لا يتغيّر إلا `SITE_URL` |
| **مفاتيح الجلسة** | في قاعدة الموقع **مشفّرةً**، لا على قرص الجهاز — فتنتقل الجلسة مع البوت بلا مسح QR جديد |
| **الربط** | لوحة صاحب المتجر ← الإعدادات ← واتساب ← «ربط واتساب» ثم مسح QR |

**سرّان لا ثالث لهما** في `bot/.env`:

| المتغيّر | من أين | ماذا لو ضاع |
|---------|--------|-------------|
| `INTERNAL_API_KEY` | لوحة Render ← خدمة `wtn` ← Environment | لا يتّصل البوت (والواجهة الداخلية مغلقة 503 بدونه) |
| `BOT_ENCRYPTION_KEY` | `npm run keygen` مرّةً واحدة | **تُفقَد كل الجلسات** ويلزم مسح QR من جديد — احتفظ بنسخة |

**البوت مطفأ ⇒ لا شيء يضيع:** الرسائل تنتظر في الطابور وتنطلق حين يعمل.
التفاصيل الكاملة واحتياطات الحظر في [bot/README.md](bot/README.md).

---

## 6. ملاحظات وقيود مهمّة

- ⏱ **الخطة المجانية تنام** بعد ~15 دقيقة خمول. أوّل فتح بعد النوم يستغرق
  ~دقيقة (إقلاع بارد) ثم يعمل بسرعته الطبيعية.
- ⚠ **لا نسخ احتياطي — بعد.** لا يوجد في المستودع أي أداة `pg_dump` أو استعادة،
  ولهذا ضاعت كل البيانات في 2026-08-12 (§1.5). قاعدة Neon لا تُحذف بمرور الوقت،
  لكن غياب النسخ الاحتياطي يبقى خطراً قائماً على البيانات.
- 🔄 **تنبيه للنسخة المحلية:** قد يكون مجلد `d:\wtn` متأخّراً عن الفرع المنشور.
  للتحقّق والمزامنة:
  ```bash
  git fetch origin
  git log --oneline HEAD..origin/main
  git pull origin main
  ```

---

## 7. التشغيل محلياً

**وضع التطوير** (خادمان منفصلان):
```bash
bash scripts/dev.sh          # ثم افتح http://localhost:5173
```

**وضع مطابق للإنتاج** (خادم واحد يقدّم الواجهة والـ API):
```bash
cd frontend && npm run build
cd ../backend && FRONTEND_DIST=../frontend/dist DEBUG=0 \
  venv/bin/gunicorn config.wsgi:application --bind 127.0.0.1:8000
```

---

## 8. ملفات ذات صلة

| الملف | المحتوى |
|------|---------|
| [docs/DEPLOY.md](docs/DEPLOY.md) | دليل النشر الأصلي المفصّل |
| [docs/START_HERE.md](docs/START_HERE.md) | توجيه أي جلسة جديدة — نظرة شاملة |
| [docs/NEXT_STEPS.md](docs/NEXT_STEPS.md) | ما تبقّى من خطوات |
| [plan.md](plan.md) | **سجلّ العمل** — ما أُنجز جلسةً جلسة |
| [plan2.md](plan2.md) | **ما تبقّى** — جرد كامل مرتّب بالأولوية (2026-08-16) |
| [bot/README.md](bot/README.md) | بوت واتساب: التشغيل · الأسرار · احتياطات الحظر |
| [docs/ROLES.md](docs/ROLES.md) | الأدوار الأربعة |
| [docs/integrations/OUR_API.md](docs/integrations/OUR_API.md) | **واجهتنا الخارجية** — كي يشتري منّا متجرٌ من خارج المنصّة |
| [docs/BUILD_LOG.md](docs/BUILD_LOG.md) | سجلّ البناء خطوة بخطوة |
