# الخطوات القادمة — Roadmap & Pending

> ملف حيّ يتتبّع ما تبقّى + الصور المطلوبة من المالك. يُحدّث مع كل خطوة.

---

## الحالة العامة
النظام الأساسي مكتمل (لوحة الأدمن + المتجر + لوحة المنصّة + التقارير)، مصقول
(أعمدة مطابقة + أيقونات SVG + خطوط Cairo/Tajawal). الآن نضبط **الأدوار الأربعة**
ونبني لوحات الأدوار الناقصة.

## الأدوار المعتمدة (4)
`platform_owner` · `tenant_admin` · `ana_bayi` (وكيل كبير) · `bayi` (وكيل).
(العميل النهائي بلا حساب · alt_bayi ليس دوراً — راجع `docs/ROLES.md`.)

---

## الخطوات (بالترتيب)

- [x] **1. تصحيح الأدوار في الكود** → حُذف `alt_bayi` و `customer`؛ 4 أدوار فقط. ✅
- [x] **2. توجيه الأدوار (Role-based landing + guards)** → كل دور يدخل للوحته
      تلقائياً، ومنع الوصول لغير المصرّح. **مُختبر للأربعة.** ✅
      (platform→/platform · tenant_admin→/dealers · ana_bayi→/bigagent · bayi→/store)
- [x] **3. لوحة الوكيل الكبير (`ana_bayi`)** → 4 تبويبات (الرئيسية · وكلائي ·
      هامش الأسعار · طلباتي) + **منطق الهامش حيّ** (سعر دكاكينه = تكلفته + هامشه)
      + بيع مباشر. **مُختبر:** UC 60 = 32 + 10% = 35.20. ✅
- [x] **4. إنشاء الوكلاء** → الوكيل الكبير ينشئ دكاكينه (✅ من تبويب "وكلائي").
      وزر "إضافة وكيل" في لوحة صاحب المتجر **مُفعّل** الآن (نموذج `DealerCreateModal`
      + `POST /dealers/`). مُختبر: إنشاء → ظهور في القائمة → دخول بدور `bayi`. ✅
- [x] **5. لوحة الوكيل العادي (`bayi`)** → مكتملة بـ6 تبويبات، كلها معزولة
      بيانات للوكيل نفسه (بلا إنشاء وكلاء / بلا مجموعات أسعار). مرجع `endagent/`.
      - [x] القسم 1: الهيكل + الرئيسية + البيع + طلباتي (`store/summary`,`store/orders`). ✅
      - [x] القسم 2: تقاريري (`store/report/` مجمّع + مجاميع + فلتر تواريخ). ✅
      - [x] القسم 3: محفظتي (`store/wallet/` رصيد+كشف حركات) + إعداداتي
            (`store/change-password/`). مُختبر end-to-end. ✅
- [x] **6. المنتجات العالمية (Global Products) + خطط الاشتراك** ✅
      - [x] مكتبة عالمية (`LibraryGame`/`LibraryProduct`) يحرّرها مالك المنصّة،
            واستيراد بضغطة (نسخ snapshot) لصاحب المتجر مع منع التكرار وحرية
            التعديل/الحذف دون التأثير على المصدر. (وصفة `reusable...import`.)
      - [x] خطط الاشتراك: مالك المنصّة يحدّد لكل متجر سعراً شهرياً/سنوياً،
            ويفعّل/يجدّد/يلغي (تمديد من نهاية الاشتراك الحالي). مُختبر end-to-end.
- [ ] **7. التنفيذ التلقائي للطلبات** → محوّلات ZNET/Barakat/Internal
      (المواصفات في `docs/integrations/PROVIDER_API_SPEC.md`) + Celery.
- [ ] **8. اللحظية (WebSockets)** · التذاكر/الرسائل · الفوترة بين المنصّة والمستأجرين.

---

## 📸 صور مطلوبة من المالك (لاحقاً — سأطلبها في وقتها)

| للخطوة | الصورة المطلوبة | الحالة |
|--------|------------------|--------|
| 3 | أعلى صفحة `Fiyat Grupları` للوكيل الكبير (عمود "نسبتي %" إن وُجد) | ⏳ اختياري |
| 5 | **متجر الوكيل** — صفحة الشراء اللي يشوفها الوكيل/الزبون (إدخال معرّف اللاعب) | ⏳ مطلوب |
| 5 | لوحة الوكيل العادي (`bayi`) — تبويباته لما يدخل | ⏳ مطلوب |
| 6 | صفحة **المنتجات العالمية** عند مالك المنصّة (إضافة منتج جاهز) | ⏳ مطلوب |
| 6 | صفحة **خطط الاشتراك** عند مالك المنصّة | ⏳ اختياري |

> الصور المتوفّرة حالياً: `reference/` (لوحة الأدمن) · `bigagent/` (لوحة الوكيل الكبير).

---

## ملاحظات تشغيلية
- البيئة السحابية مؤقتة وتُطفئ الخوادم — الكود آمن على GitHub بعد كل خطوة.
- التوثيق: `BUILD_LOG.md` (سجل البناء) · `ROLES.md` (الأدوار) · هذا الملف (القادم).
