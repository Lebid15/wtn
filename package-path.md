# package-path — مسار الباقة

> ملاحظات حول **توجيه الباقة**: كيف يسافر الطلب من ضغطة الوكيل حتى المزوّد
> الخارجي، وأين يتعطّل. يُحدَّث مع كل تجربة حيّة.

**آخر فحص:** 2026-07-31 · على الموقع الحيّ <https://wtn.onrender.com>

---

## 1. حالة التجربة الجارية

| العنصر | القيمة الفعلية (مقروءة من الموقع الحيّ) |
|--------|------------------------------------------|
| **الباقة** | `PUBG Mobile / 60 UC` — المنتج `id=1` |
| التكلفة / السعر | `28.00` / `32.00` |
| نوع التنفيذ | `auto` (تلقائي) ✅ |
| **المزوّد الرئيسي** | `alaya` — المزوّد `id=10` |
| API 1 / API 2 | `null` / `null` (بديل مغلق) — لا سلسلة احتياطية بعد |
| **رقم الربط لدى المزوّد** | `oyun=1` · `kupur=60` ✅ (محفوظ في `ProductLink`) |
| **الوكيل** | `abd shop` — المستخدم `id=4` · الدخول `bayi003` |
| رصيد الوكيل | `25,848.15` (كافٍ) |

### المزوّد `alaya` (id=10)
```json
{ "code": "znet", "base_url": "http://bayi.alayatl.com/", "kod": "5442199998", "sifre": "***" }
```
النوع في القاعدة `card_store` لكن `config.code = "znet"` — و`adapter_for()` يعطي
**الكود الصريح أولوية على النوع**، فالمحوّل المختار هو `ZnetAdapter` ✅.

---

## 2. المسار الكامل للباقة (خطوة بخطوة)

```
الوكيل abd shop يضغط "شراء"
      │
      ▼
POST /api/store/buy/            ← أو POST /api/orders/ من لوحة الأدمن نيابةً عنه
      │                            (backend/orders/views.py)
      ▼
services.create_order()         ← ينشئ الطلب + يخصم المحفظة فوراً + قيد في دفتر الأستاذ
      │
      ▼
_maybe_auto_execute(order)      ← الشرط: execution_type == auto  و  وجود مزوّد واحد على الأقل
      │
      ▼
services.dispatch_order()       ← يبني السلسلة: [provider, provider_alt1, provider_alt2]
      │                            هنا السلسلة = [alaya] فقط
      ▼
registry.adapter_for(alaya)     ← config.code == "znet"  →  ZnetAdapter
      │
      ▼
ZnetAdapter.place_order()       ← GET خارجي فعلي إلى bayi.alayatl.com
      │
      ▼
parse_place(response)           ← "OK|cost|balance" → قيد التنفيذ · غير ذلك → فشل
      │
      ├─ success     → الطلب "ناجح" + PIN
      ├─ processing  → الطلب "قيد التنفيذ" ← **توقف السلسلة، لا يُجرَّب البديل**
      └─ failed      → يُجرَّب البديل التالي … وإن فشل الكل → "عالق"
```

### الطلب الفعلي الذي يخرج إلى ZNET
```
GET http://bayi.alayatl.com/servis/pin_ekle.php
    ?kod=5442199998
    &sifre=***
    &oyun=auto-demo            ← من product.provider_package_id
    &referans=<timestamp_ms + رقم عشوائي>
    &musteri_tel=<customer_phone أو player_id>
    &oyuncu_bilgi=<player_id>
```

### استعلام الرصيد (زر ☀ — بلا تكلفة)
```
GET http://bayi.alayatl.com/servis/bakiye_kontrol.php?kod=5442199998&sifre=***
الردّ المتوقّع: OK|الرصيد|الدين
```

---

## 3. فحص الاتصال بـ ZNET

### المحاولة الأولى — 2026-07-31 (قبل تصحيح بيانات المزوّد)
`POST /api/providers/10/refresh-balance/` → `HTTP 502` · `{"detail": "API_BAGLANTI_HATASI"}`

`API_BAGLANTI_HATASI` نصّ **تركي صادر عن ZNET نفسه**، لا خطأ شبكة من طرفنا (لو
تعذّر الوصول لكانت الرسالة `تعذّر الاتصال بـ ZNET: …` من مسار `RequestException`).
أي أن خادم Render وصل فعلاً إلى `bayi.alayatl.com` وZNET ردّ بالرفض.

### المحاولة الثانية — 2026-07-31 (بعد تصحيح `kod`/`sifre`) ✅
```
POST /api/providers/10/refresh-balance/  →  HTTP 200
real_balance = 625.00   ·   debt = 2100.00
```
**الاتصال والاعتماد سليمان.** المسار من الوكيل حتى بوّابة ZNET مفتوح بالكامل.

---

## 4. العوائق (ترتيب الأولوية)

### ✅ عائق 1 — اعتماد ZNET — **حُلّ**
كان `API_BAGLANTI_HATASI`. صُحّحت بيانات المزوّد (`kod`/`sifre`) فصار جلب
الرصيد ناجحاً. (كانت الفرضيات المطروحة: قيد IP · API غير مفعّل · بيانات خاطئة —
والسبب الفعلي كان البيانات.)

### ✅ عائق 4 — بذرة النشر تدهس التوجيه — **حُلّ**
`start.sh` يشغّل `seed_auto` في **كل نشر**، وكان يكتب بلا شرط على **أول منتج
بالترتيب** — وهو بالضبط `PUBG Mobile / 60 UC`:
```python
product.provider = بنك البينات الآلي
product.execution_type = auto
product.provider_package_id = "auto-demo"
```
أي أن أي رقم ربط يضبطه صاحب المتجر كان يُمسح صامتاً عند النشر التالي، ويُعاد
توجيه الباقة من `alaya` إلى بنك البينات. **الإصلاح:** إن كان للمنتج أي مزوّد
(رئيسي أو بديل) أو رقم ربط ⇒ تُتخطّى البذرة. تعمل على قاعدة جديدة فقط.

### ✅ عائق 5 — `Kupur Bilgisi Bulunamadı` — **حُلّ**
التجربة الحيّة (طلب `26073073246`) وصلت ZNET فعلاً وردّ:
`Kupur Bilgisi Bulunamadı` («معلومات الكوبون غير موجودة»).

**السبب:** `pin_ekle.php` يطلب معاملَين — `oyun` (معرّف اللعبة) **و** `kupur`
(كود الكوبون) — كما في `docs/integrations/PROVIDER_API_SPEC.md`. محوّلنا كان
يرسل `oyun` فقط ولا يرسل `kupur` إطلاقاً.

**تأكيد من كتالوج ZNET الحيّ** (`pin_listesi.php` → 752 باقة):
```
{"id": "1", "name": "PUBG Mobile 60 UC", "game": "PUBG", "kupur": "60", "price": "43.40"}
```
ZNET يعطي **`oyun` واحداً لكل لعبة** (PUBG=1 · PUBG Lite=23 · Pubg TR=157)
ويميّز الباقات داخلها بـ `kupur`. إذاً القيمة `60` هي **kupur** لا **oyun**.

**الإصلاح:** المحوّل صار يرسل `kupur` أيضاً (من `link.extra` أو من كوبور المنتج).

### ✅ عائق 6 — رقم ربط واحد لثلاثة مزوّدين — **حُلّ**
`provider_package_id` كان حقلاً واحداً على المنتج، بينما المنتج يُوجَّه إلى
ثلاثة مزوّدين وكلٌّ يرقّم الباقة كما يشاء. أُضيف نموذج `ProductLink`:
رقم ربط مستقلّ لكل **(باقة × مزوّد)** + `extra` لما يحتاجه المزوّد زيادةً
(`kupur` لـ ZNET). مع ارتداد للحقل القديم فلا تنكسر الإعدادات القائمة.

### 🔴 عائق 7 — **التسعير خاسر** (تجاري، لا تقني)
| | القيمة |
|---|---|
| تكلفة 60 UC لدى ZNET | **43.40** |
| `cost_price` في نظامنا | 28.00 |
| سعر البيع للوكيل | 32.00 |
| **النتيجة لكل طلب** | **خسارة ≈ 11.40** |

كل عملية بيع ناجحة عبر هذا التوجيه تخسر مالاً. يلزم تحديث التكلفة والسعر
قبل تفعيل البيع الفعلي على هذه الباقة.

### 🟠 عائق 8 — `API 1` = المزوّد الرئيسي نفسه
`provider_alt1 = alaya` وهو عين الرئيسي، فالنظام يعيد المحاولة على المزوّد
ذاته بلا فائدة (ظهر الخطأ مرّتين في سجلّ الطلب). يُفضّل «بديل مغلق» أو
مزوّد مختلف فعلاً.

### 🟠 عائق 2 — `provider_package_id = "auto-demo"`
قيمة تجريبية من بذرة `seed_auto`. تُرسَل في المعامل `oyun`، وZNET لا يعرفها.
حتى لو حُلّ العائق الأول، سيفشل الطلب. **نحتاج معرّف الباقة الحقيقي (`oyun` id)
لـ 60 UC من لوحة ZNET.**

### 🟡 عائق 3 — لا يوجد بديل
`API 1` و`API 2` فارغان. أي فشل لدى `alaya` ⇒ الطلب **"عالق"** فوراً.

### 🔵 ملاحظة أمنية — `http://` وليس `https://`
`kod` و`sifre` يُرسلان في **query string عبر HTTP غير مشفّر**. يُنصح بتحويل
`base_url` إلى `https://` إن كان مدعوماً لدى المزوّد.

---

## 5. سلوكيات مهمّة يجب تذكّرها

- **الخصم يحدث قبل الإرسال** — `create_order` يخصم محفظة الوكيل فوراً.
- **فشل التوجيه** (لم يصل مزوّداً) لا يسترجع آلياً — القرار للأدمن (زر ✗).
  أمّا **إلغاء المزوّد بعد القبول** (ZNET حالة 3) فيسترجع تلقائياً، لأن
  المزوّد يعيد المبلغ إلى رصيدنا لديه فلا معنى لحجز مال الوكيل.
- **"قيد التنفيذ" توقف السلسلة** — لأن الطلب وصل مزوّداً فعلاً؛ لا يُجرَّب البديل.
- **ZNET لا يحسم الطلب فوراً** — يؤكّد الاستلام فقط (`OK|تكلفة|رصيد`)، ثم
  يحسمه المشغّل قبولاً أو إلغاءً. ✅ **حلقة المراقبة مبنيّة الآن:**
  `sync_pending` تستعلم `pin_kontrol` كل 6 ثوانٍ ما دام هناك طلب قيد التنفيذ،
  وتتوقّف وحدها عند خلوّ القائمة.
- **النجاح بلا PIN حالة صحيحة** — الباقات التي تُشحن على معرّف اللاعب مباشرةً
  لا كود لها؛ يُعرض «شُحن مباشرةً ✓» مع ملاحظة المزوّد.
- **حماية الحلقات:** التوجيه الداخلي (متجر ← متجر) محدود بعمق 2.

---

## 6. الخطوة التالية (بانتظار بياناتك)

| # | المطلوب | من | الحالة |
|---|---------|-----|--------|
| 1 | تصحيح اعتماد ZNET (`kod`/`sifre`) | أنت | ✅ تمّ — الرصيد 625.00 |
| 2 | **رقم الربط الحقيقي** (`oyun` id) لباقة 60 UC من لوحة ZNET | أنت | ⏳ مطلوب |
| 3 | معرّف لاعب للتجربة (`player_id`) | أنت | ⏳ مطلوب |
| 4 | ضبط رقم الربط من ⚙ ثم إنشاء الطلب لـ `abd shop` وتتبّعه | أنا | ⏳ بانتظار 2 و 3 |

> **تنبيه:** الطلب الناجح **حقيقي** — يستهلك رصيداً فعلياً من حساب ZNET
> ويشحن UC فعلياً للاعب. ليس محاكاة.

---

## 7. سجلّ الفحوص

| # | التاريخ | الفحص | النتيجة |
|---|---------|-------|---------|
| 1 | 2026-07-31 | قراءة إعداد المنتج 1 والمزوّد 10 والوكيل 4 من الموقع الحيّ | ✅ كل الروابط سليمة · `provider_package_id="auto-demo"` |
| 2 | 2026-07-31 | `refresh-balance` على المزوّد 10 (ZNET) | ❌ `API_BAGLANTI_HATASI` — وصلنا لـ ZNET وردّ بالرفض |
| 3 | 2026-07-31 | `refresh-balance` بعد تصحيح `kod`/`sifre` | ✅ `HTTP 200` — رصيد 625.00 · دين 2100.00 |
| 4 | 2026-07-31 | مراجعة `seed_auto` (تعمل في كل نشر) | 🔴 تدهس توجيه المنتج 1 → أُصلحت |
| 5 | 2026-07-31 | تفعيل زرّي ✏ و ⚙ في جدول عمليات المنتجات | ✅ نُشر — البناء `index-BS5A5xcf.js` حيّ |
| 6 | 2026-07-31 | اختبار مسار حفظ النافذتين على الموقع الحيّ (11 فحصاً) | ✅ كلها خضراء · أُعيدت حالة المنتج كما كانت |
| 7 | 2026-07-31 | **طلب فعلي** لـ `abd shop` (فيش `26073073246`) | ⚠️ وصل ZNET وردّ `Kupur Bilgisi Bulunamadı` · الطلب **عالق** · خُصم 31.00 بلا استرجاع |
| 8 | 2026-07-31 | `GET /providers/10/packages/` (كتالوج ZNET) | ✅ **752 باقة** — منها `oyun=1 · kupur=60 · 43.40` لـ PUBG 60 UC |
| 9 | 2026-07-31 | كتالوج بنك البينات ومتجر المورّد | ✅ مجموعة واحدة (10 بينات) · منتج واحد (25.00) |
| 10 | 2026-07-31 | طلب `26073180840` بـ `oyun=1 · kupur=60` | ✅ **قبِله ZNET**: `OK\|43.40\|581.60` · الرصيد 625.00→581.60 |
| 11 | 2026-07-31 | إبطال المالك للطلب من لوحة ZNET | ✅ التقطته حلقة المراقبة → «عالق» · ZNET أعاد المبلغ (581.60→625.00) |
| 12 | 2026-07-31 | استلام وثيقة ZNET الرسمية | ✅ حُفظت في [`docs/integrations/ZNET_API.md`](docs/integrations/ZNET_API.md) — `3 = IPTAL` |
| 14 | 2026-07-31 | طلب `26073132942` — **قبول** من اللوحة | ✅ `OK\|2\|\|هذا المنتج تم شحنه عالميا` → **ناجح** + وصلت الملاحظة |
| 13 | 2026-07-31 | طلب `26073113565` — دورة كاملة | ✅ إرسال → `OK\|1\| - \|` → بعد 12s `OK\|3\| - \|` → **ملغى + استرجاع تلقائي** |

### تفصيل التجربة 13 (الدورة الكاملة المؤكَّدة)
```
0s   إرسال    → OK|43.40|581.60 · ref=1785462437160 · الحالة: قيد التنفيذ
0s   استعلام  → OK|1| - |   (قيد المعالجة · بلا açıklama)
12s  استعلام  → OK|3| - |   (IPTAL · بلا açıklama)
     النتيجة  → الطلب "ملغى" + استرجاع فوري
```
**دفتر الأستاذ يثبت الدورة المالية الكاملة:**
```
خصم طلب  -31.00  «طلب 60 UC»              01:47
استرجاع  +31.00  «إلغاء طلب 26073113565»  01:47
```
صافي أثر على محفظة الوكيل = **صفر** · وصافي أثر على رصيد ZNET = **صفر**.

### تفصيل التجربة 14 — **مسار النجاح مثبَتاً**
```
0s   إرسال    → OK|43.40|581.60 · ref=1785462608149 · قيد التنفيذ
0s   استعلام  → OK|1| - |                          (قيد المعالجة)
24s  استعلام  → OK|2||هذا المنتج تم شحنه عالميا     (ناجح + ملاحظة المشغّل)
     النتيجة  → الطلب "ناجح" · رد النظام: «المزوّد أكّد التنفيذ · هذا المنتج تم شحنه عالميا»
```
| | قبل | بعد |
|---|---|---|
| رصيد ZNET | 625.00 | **581.60** (خُصم 43.40) |
| محفظة `abd shop` | 25880.15 | **25849.15** (خُصم 31.00) |

**تصحيحان لملاحظة سابقة:**
1. **`Açıklama` يصل فعلاً** حين يكتبه المشغّل. فراغه عند الإلغاء كان لأنه لم
   يُكتب نصّ — لا لأن ZNET لا يرسله.
2. **النجاح بلا PIN حالة صحيحة:** الباقة تُشحن مباشرةً على معرّف اللاعب فلا
   كود لها. عُولج العرض ليقول «شُحن مباشرةً ✓» مع ملاحظة المزوّد.
