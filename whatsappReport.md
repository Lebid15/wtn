تقرير: كيف تم ربط الواتساب في مشروع «مطابقة» — دليل مرجعي للمشروع الثاني
الغرض من هذا التقرير: شرح المعمارية والآلية التي استخدمناها لربط الواتساب في مشروعنا الحالي (منصّة الصرافة/المطابقة)، بحيث يستطيع مهندس مشروعك الثاني (موقع شحن التطبيقات والألعاب) أن يبني نفس النظام، مع تطبيق فكرتك: زر إرسال رسالة رصيد بجانب كل وكيل + زر إرسال جماعي لكل وكيل رصيده سالب.

هذا مستند توثيقي فقط. لم يُعدَّل أي كود في المشروع الحالي.

0) الخلاصة في سطور (للمدير التقني المستعجل)
الربط يتم عبر مكتبة Baileys (Node.js) التي تتحدث ببروتوكول واتساب ويب؛ كل رقم واتساب = «جهاز مرتبط» (Linked Device) بعد مسح QR مرة واحدة.
المعمارية ثلاث خدمات منفصلة: Backend (Django) + Bot (Node/Baileys) + Frontend (React)، تتخاطب عبر HTTP داخلي محمي بـ مفتاح سرّي مشترك.
العزل هو حجر الأساس: كل «صاحب نسخة/مدير» = Tenant مستقل له جلسة واتساب خاصة ومجلد اعتماد مشفّر خاص وبياناته معزولة تماماً في قاعدة البيانات.
الإرسال اليدوي (الذي تريد بناء الزر عليه) موجود أصلاً عندنا: الواجهة تنادي Django، وDjango يبني نص الرسالة من قالب يُعدّه المدير، ثم يطلب من البوت POST /sessions/:tenantId/send فيرسلها البوت للمجموعة/الوكيل.
تحذير مهم بخصوص الإرسال الجماعي: Baileys غير رسمي، والإرسال الجماعي الدفعي هو أكثر ما يُسبّب حظر الرقم. الميزة ممكنة تقنياً لكن يجب تنفيذها بتهدئة وتأخير عشوائي ودُفعات صغيرة (تفاصيل في القسم 9).
1) الصورة الكبيرة: ما الذي يفعله الربط؟
في مشروعنا، الرقم الثاني (رقم البوت) يُضاف إلى مجموعات واتساب مع العملاء. البوت:

يقرأ كل رسالة تصل للمجموعة فور وصولها (عبر اتصال WebSocket دائم مع خوادم واتساب).
يرسلها إلى Django ليحللها ويحفظها كقيد (Ledger Entry) ويحدّث الرصيد.
يرد تلقائياً على أوامر معيّنة (مثل «مطابقة») برسالة فيها الأرصدة.
يُرسل رسائل يدوية عند الطلب من لوحة التحكم (زر «إرسال مطابقة» بجانب كل مجموعة) — وهذا بالضبط ما ستبني عليه فكرتك.
في مشروعك الثاني، بدّل «المجموعة» بـ «الوكيل»، و«القيد اليومي» بـ «رصيد الوكيل»، وستحصل على نفس النموذج.

2) المعمارية العامة (ثلاث خدمات)
┌──────────────────┐   HTTPS    ┌──────────────────────────┐
│  Frontend (React)│ ─────────▶ │      Backend (Django)     │
│  لوحة تحكم المدير │ ◀───────── │  - منطق الأعمال والأرصدة   │
└──────────────────┘            │  - القوالب لكل مدير        │
                                │  - يصدر أوامر للبوت        │
                                └───────┬──────────▲─────────┘
                        HTTP داخلي       │          │  HTTP داخلي
                   (Django → Bot: أرسِل) │          │ (Bot → Django: استوعب)
                                         ▼          │
                                ┌──────────────────────────┐
                                │   Bot (Node.js + Baileys) │
                                │  مدير جلسات: جلسة لكل مدير  │
                                └───────┬──────────▲─────────┘
                                        │ WebSocket │
                                        ▼           │
                                ┌──────────────────────────┐
                                │      خوادم واتساب          │
                                └──────────────────────────┘
الخدمات الثلاث تُشغَّل عبر docker-compose (عندنا: db PostgreSQL + backend + bot + frontend). أهم متغيّرات البيئة المشتركة:

المتغيّر	يوجد في	الوظيفة
INTERNAL_API_KEY	backend + bot	السرّ المشترك الذي يحمي كل الاتصالات الداخلية بين Django والبوت (بالاتجاهين).
BOT_URL (http://bot:3100)	backend	عنوان خادم البوت الداخلي.
DJANGO_URL (http://backend:8000)	bot	عنوان Django ليرسل له البوت الرسائل الواردة.
BOT_ENCRYPTION_KEY (base64، 32 بايت)	bot	مفتاح تشفير ملفات جلسات واتساب على القرص (AES‑256‑GCM).
3) لماذا Baileys؟ (الاختيار التقني)
Baileys (@whiskeysockets/baileys) مكتبة Node.js مفتوحة المصدر، تنفّذ بروتوكول واتساب ويب عكسياً. لا متصفح ولا Chrome — أخفّ وأسرع من whatsapp-web.js.
تدعم قراءة المجموعات وإرسال نص وصور ومستندات، وهذا ضروري لحالتنا (وحالتك).
البديل الرسمي (WhatsApp Cloud API من Meta) لا يصلح لأنه لا يقرأ رسائل المجموعات ولا يبادر بالرسائل إلا ضمن نافذة 24 ساعة بعد أن يراسلك العميل أولاً. لذلك اخترنا Baileys.
المقابل: Baileys غير معتمد رسمياً من واتساب → خطر حظر الرقم قائم إذا ظهر نمط «آلي» (سرعة عالية، رسائل جماعية، سبام). التخفيف في القسم 9.
المكتبات المستخدمة عندنا في البوت:

"@whiskeysockets/baileys": "^6.7.9",   // اتصال واتساب
"express": "^4",                        // خادم HTTP داخلي يستقبل أوامر Django
"qrcode": "^1.5",                       // توليد صورة QR كـ data URL
"pino": "^9",                           // سجلات
"@sentry/node": "^8"                    // (اختياري) رصد الأخطاء
4) نموذج العزل بين المدراء — أهم قسم بالنسبة لك
عندك: أنت تبيع نسخاً لمدراء، وتحت كل مدير وكلاء، ويجب عزل كل مدير عن الآخر تماماً. عندنا نفس الفكرة تماماً، وهذا كيف حللناها:

4.1 الوحدة المعزولة = Tenant
عندنا كيان اسمه Tenant (= «مكتب صرافة» = عندك «مدير/صاحب نسخة»). كل مستخدم-مدير مرتبط بـ Tenant واحد عبر علاقة OneToOne. كل بيانات المدير معزولة تحت هذا الـ Tenant: المجموعات/الوكلاء، القيود، الأرصدة، القوالب، إعدادات الواتساب.

4.2 معرّف الجلسة = tenant_id
هذه هي الحيلة المحورية: جلسة واتساب لكل مدير مُعرّفة برقم الـ Tenant.

في البوت هناك SessionManager يحتفظ بـ Map<tenantId, Session> — أي جلسة واتساب مستقلة لكل مدير في الذاكرة.
ملفات اعتماد كل جلسة تُخزّن في مجلد منفصل: auth_sessions/<tenant_id>/ (مشفّرة).
كل نداءات Django للبوت تحمل tenant_id في المسار: POST /sessions/<tenant_id>/start, GET /sessions/<tenant_id>, POST /sessions/<tenant_id>/send ... إلخ.
بهذا الشكل: مدير A لا يستطيع بأي حال رؤية أو استخدام جلسة مدير B، لأن كل استعلام وكل مجلد وكل نداء API مقيّد بـ tenant_id الخاص به.

4.3 العزل على مستوى قاعدة البيانات
كل جدول أساسي فيه مفتاح خارجي tenant. وكل استعلام في الواجهة يُفلتَر تلقائياً بالـ tenant الخاص بالمستخدم المسجّل، مثل:

tenant = getattr(request.user, "tenant", None)
qs = Group.objects.filter(tenant=tenant)   # لا يرى إلا مجموعات/وكلاء مكتبه
وحتى النداءات الداخلية من البوت تُحمّل الـ Tenant من tenant_id وتضعه على request.tenant، فلا يمكن لبيانات مدير أن تتسرّب لآخر.

للمشروع الثاني: أنشئ كياناً مكافئاً (سمِّه Reseller أو Manager أو Tenant)، واجعل tenant_id هو مفتاح جلسة واتساب. اجعل «الوكلاء» جدولاً يتبع الـ Tenant. طبّق الفلترة بالـ tenant في كل استعلام. هذا يمنحك نفس العزل الصارم.

5) آلية جلسة واتساب (الربط، QR، الاستمرارية)
5.1 دورة حياة الجلسة
لكل جلسة حالة واضحة: idle → qr → connecting → connected → closed.

المدير يضغط «ربط واتساب» في الواجهة.
الواجهة تنادي POST /whatsapp/connect/ في Django.
Django ينادي البوت POST /sessions/<tenant_id>/start.
البوت يُنشئ makeWASocket(...)، فيصدر واتساب رمز QR؛ يحوّله البوت إلى صورة data:image/png;base64,....
الواجهة تستفتي (poll) كل ثانيتين GET /whatsapp/status/ وتعرض الـ QR.
المدير يمسح الـ QR من هاتف الرقم الثاني (واتساب ← الأجهزة المرتبطة).
تتحوّل الحالة إلى connected، ويختفي الـ QR، ويُخزَّن رقم الجهاز.
5.2 تخزين الاعتماد مشفّراً (مهم للأمان)
جلسة واتساب = ملفات مفاتيح حسّاسة على القرص. عندنا طبقة تخزين مخصّصة useEncryptedMultiFileAuthState تشفّر كل ملف قبل كتابته:

AES‑256‑GCM، مفتاح مشتق بـ HKDF‑SHA256 (salt = اسم الملف)، ونونس عشوائي 12 بايت + وسم مصادقة 16 بايت لكل ملف.
المفتاح الأساسي من BOT_ENCRYPTION_KEY (32 بايت base64) في البيئة، لا يُخزَّن على القرص.
متوافقة رجعياً مع الجلسات القديمة غير المشفّرة (هجرة تدريجية عند أول كتابة).
لو سُرق القرص/النسخة الاحتياطية دون المفتاح، تبقى الجلسات غير قابلة للاستخدام. أوصي بشدّة أن يطبّق المشروع الثاني نفس التشفير؛ ملفات جلسة واتساب المكشوفة = اختطاف حساب.

5.3 الاستمرارية وإعادة الاتصال
عند إقلاع البوت، bootstrap() يقرأ كل مجلدات auth_sessions/* ويُوقظ جلسة كل مدير تلقائياً (لا حاجة لإعادة مسح QR بعد إعادة تشغيل الخادم).
عند انقطاع غير مقصود: يُعيد المحاولة تلقائياً بعد ثانيتين.
عند تسجيل خروج فعلي من هاتف المستخدم (loggedOut): يُمسح مجلد الاعتماد بالكامل لأن مفاتيحه أصبحت فاسدة، فتبدأ عملية ربط جديدة نظيفة بـ QR جديد.
خيارات مهمّة عند إنشاء السوكِت: markOnlineOnConnect: false (لا يظهر البوت «متصل» دائماً)، syncFullHistory: false (لا يسحب كل التاريخ)، واسم متصفح مخصّص ['mutabaka','Chrome','1.0'].
6) عقد الاتصال الداخلي (API بين Django والبوت)
قناتان، كلتاهما محميّتان بنفس السرّ المشترك INTERNAL_API_KEY في الترويسة X-Internal-Api-Key، مع مقارنة آمنة ضد هجمات التوقيت (timing‑safe) على الطرفين.

6.1 من Django ← إلى البوت (التحكم بالجلسة والإرسال)
خادم البوت (Express) يوفّر:

الطريقة والمسار	الوظيفة
POST /sessions/:tenantId/start	بدء/استئناف جلسة (يُعيد الحالة و QR إن وُجد).
GET  /sessions/:tenantId	حالة الجلسة (idle/qr/connecting/connected/closed) + الـ QR.
POST /sessions/:tenantId/logout	تسجيل خروج وحذف الجلسة.
DELETE /sessions/:tenantId	حذف نهائي (logout + مسح مجلد الاعتماد).
POST /sessions/:tenantId/send	إرسال رسالة نصّية { group_id: JID, text } — أساس زر الإرسال عندك.
POST /sessions/:tenantId/send-image	إرسال صورة/مستند base64 { group_id, image_b64, mimetype, caption }.
GET  /healthz	فحص صحة + عدد الجلسات.
عند الإرسال، يتحقّق البوت أن الجلسة connected؛ وإلا يرد 409 بحالتها الحالية (مهم لتظهر للمدير رسالة «واتساب غير متصل»).

6.2 من البوت ← إلى Django (استيعاب الرسائل الواردة)
عميل البوت ينادي:

المسار	الوظيفة
POST /api/internal/ingest/	يرسل الرسالة الواردة ليحللها Django ويحفظها كقيد ويحدّث الرصيد.
POST /api/internal/command/	يفحص إن كانت الرسالة أمراً (مطابقة/مساعدة) ويُعيد نص الرد.
POST /api/internal/reconcile-log/	يسجّل نص المطابقة المُرسَل للعرض فقط.
على جانب Django، صلاحية IsInternalApiKey تتحقّق من السرّ، ثم تُحمّل Tenant من tenant_id الموجود في جسم الطلب وتضعه على request.tenant — فيبقى العزل قائماً حتى في النداءات الداخلية.

7) تدفّق الرسالة الواردة (اختياري لك — للفهم الكامل)
عند وصول رسالة لمجموعة يكون فيها رقم البوت:

البوت يلتقطها عبر messages.upsert، ويتجاهل: الرسائل الخاصة (غير المجموعات)، ورسائله هو (fromMe)، والرسائل الفارغة.
يستخرج: النص، معرّف المجموعة (JID)، المرسِل، اسمه، معرّف الرسالة، ومعرّف الرسالة المقتبَسة إن وُجد.
يستدعي ingest (تحليل + حفظ)، ثم command (هل هو أمر؟).
إن كان هناك رد، يُرسله بعد تأخير عشوائي بشري (1.5–4 ثوانٍ) لتقليل خطر الحظر.
في مشروعك (شحن تطبيقات) قد لا تحتاج قراءة المجموعات إطلاقاً — قد يكفيك الإرسال فقط (زر الرصيد + الإرسال الجماعي). في هذه الحالة تستطيع الاكتفاء بالجزء الخاص بالإرسال (القسم 6.1) وإهمال منطق الاستيعاب.

8) القوالب التي يُعدّها المدير (بالضبط ما طلبته)
عندنا، نص رسالة المطابقة يصمّمه المدير ويُحفظ على مستوى الـ Tenant، ثم يُحقن فيه الرصيد آلياً. الحقول عندنا:

reconcile_template: القالب العام، فيه العنصر النائب {LINES} الذي تُحقن مكانه الأسطر.
reconcile_line_us / reconcile_line_them: صيغة سطر «لنا» و«لكم»، بعناصر نائبة {AMOUNT} و{CURRENCY} و{FLAG}.
currency_labels / currency_flags: تسميات وأعلام العملات.
الواجهة توفّر للمدير معاينة حيّة للقالب قبل الحفظ (نداء POST بمعطيات تجريبية يُعيد النص المُركّب دون حفظ).

للمشروع الثاني: خزّن على مستوى المدير قالباً نصّياً فيه عناصر نائبة مثل:

عزيزي {AGENT}، رصيدك الحالي {BALANCE} {CURRENCY}. الرجاء التسديد. شكراً لتعاملك معنا.
ثم يبني الـ Backend النص باستبدال العناصر النائبة بقيم الوكيل الفعلية، ويمرّره للبوت عبر /sessions/:tenantId/send. هكذا يتحكّم كل مدير بنصّه بالكامل ويبقى معزولاً عن غيره.

9) تطبيق فكرتك في المشروع الثاني — التصميم المقترح
عندنا سابقة جاهزة تُطابق فكرتك: إجراء send-reconcile على مستوى «المجموعة» يبني تقرير الرصيد ويرسله فوراً عبر واتساب. عندك «المجموعة» = «الوكيل». إليك التصميم:

9.1 ربط الوكيل برقم واتساب (JID)
كل وكيل عنده يجب أن يحمل حقل رقم واتساب. صيغة الـ JID في واتساب:

محادثة فردية: <countrycode><number>@s.whatsapp.net (مثال: 9639xxxxxxx@s.whatsapp.net).
مجموعة: <...>@g.us.
بما أن الوكيل شخص فردي، غالباً ستستخدم @s.whatsapp.net. (يُفضّل التحقق من أن الرقم مسجّل على واتساب قبل الإرسال عبر onWhatsApp() في Baileys.)

9.2 الزر بجانب كل وكيل (إرسال فردي)
تدفّق مطابق لـ send-reconcile عندنا:

واجهة المدير: زر «تنبيه بالرصيد» بجانب الوكيل
        │  POST /api/agents/<id>/notify-balance/   (بجلسة المدير المصادَق عليها)
        ▼
Backend (Django/Node):
  1) تأكّد أن الوكيل يتبع هذا المدير (العزل).
  2) احسب رصيد الوكيل (مثلاً -500 USD).
  3) ابنِ النص من قالب المدير:
     "عزيزي {AGENT}، رصيدك -500 USD وعليك التسديد."
  4) نادِ البوت:  POST /sessions/<managerTenantId>/send
     body: { group_id: "<agentJid>@s.whatsapp.net", text }
        ▼
Bot: يتحقق أن جلسة هذا المدير connected، ثم sock.sendMessage(jid, { text })
على جانب Django عندنا، جوهر الاستدعاء سطر واحد:

from apps.accounts.bot_client import send_message
send_message(tenant.id, agent_jid, text)   # → POST /sessions/<tenant.id>/send
9.3 زر الإرسال الجماعي (لكل وكيل رصيده سالب)
زر أعلى جدول الوكلاء:

# 1) اجلب وكلاء هذا المدير فقط ممن رصيدهم سالب (العزل + الفلترة)
agents = Agent.objects.filter(manager=request.user.tenant, balance__lt=0)

# 2) أرسل لكل واحد مع تهدئة (throttle) — لا دفعة واحدة سريعة!
for i, agent in enumerate(agents):
    text = render_template(manager.template, agent)      # قالب المدير
    try:
        send_message(tenant.id, agent.jid, text)
    except BotClientError:
        # سجّل الفشل وتابع (مثلاً الجلسة انقطعت)
        ...
    time.sleep(random.uniform(4, 10))                    # تأخير عشوائي بين كل رسالة
الأفضل: لا تُنفّذ الإرسال الجماعي داخل طلب HTTP واحد (سيتجمّد ويسقط بمهلة). نفّذه كـ مهمّة خلفية (طابور/عامل: Celery أو BullMQ)، بحيث ترد الواجهة فوراً «تم جدولة الإرسال لـ N وكيل»، ويعمل العامل على مهله مع التأخير العشوائي.

9.4 نقاط عزل يجب ألّا تُنسى
كل عملية إرسال تستخدم جلسة المدير صاحب الطلب فقط (tenant_id من المستخدم المصادَق عليه) — لا تسمح أبداً بتمرير tenant_id من العميل.
قبل الإرسال لأي وكيل، تحقّق أنه فعلاً يتبع هذا المدير.
القالب يُقرأ من إعدادات هذا المدير فقط.
10) المخاطر والاحتياطات (اقرأها قبل الإرسال الجماعي)
Baileys غير رسمي، والإرسال الجماعي هو أعلى مصادر خطر الحظر. لاحظ أن مستند الفكرة الأصلي في مشروعنا صنّف «الرسائل الجماعية» كسلوك يرفع احتمال الحظر. بما أنك تحتاج هذه الميزة، طبّق التخفيفات التالية إلزامياً:

الخطر	التخفيف
حظر الرقم بسبب نمط آلي	استخدم رقماً ثانوياً مخصّصاً، ويُفضّل WhatsApp Business؛ لا تستخدم رقماً شخصياً مهمّاً.
سرعة الإرسال العالية	تأخير عشوائي 4–10 ثوانٍ بين كل رسالة (نحن نستخدم 1.5–4ث للردود)، ودفعات صغيرة.
إرسال دفعة ضخمة مرّة واحدة	قسّم على دفعات (مثلاً 20–30 رسالة، ثم توقّف عدة دقائق)، ويُفضّل توزيعها على اليوم.
رقم جديد يُرسل كثيراً فجأة	تسخين الرقم تدريجياً: نشاط بشري طبيعي أياماً قبل تشغيل الإرسال الآلي، وزيادة الحجم بالتدريج.
الإرسال لأرقام غير مسجّلة على واتساب	تحقّق بـ onWhatsApp() قبل الإرسال، وتخطَّ غير المسجّلين.
رسائل متطابقة حرفياً للجميع (تبدو سبام)	نوّع النص قليلاً لكل وكيل (الاسم، الرصيد، وربما جملة ختامية متغيّرة).
انقطاع الجلسة أثناء الإرسال الجماعي	تعامَل مع 409 (غير متّصلة): أوقف الدفعة، أبلغ المدير، واستأنف بعد إعادة الاتصال.
تسريب ملفات الجلسة	شفّر مجلد الاعتماد (AES‑256‑GCM كما عندنا) واحفظ المفتاح في البيئة فقط.
توصية صريحة: أبلغ صاحب المشروع الثاني أن الإرسال الجماعي عبر Baileys يحمل خطر حظر حقيقياً مهما احتطنا. إن كان الحجم كبيراً جداً أو حرِجاً تجارياً، فالخيار الأكثر أماناً (وإن كان أغلى وأقل مرونة) هو WhatsApp Cloud API الرسمي مع قوالب معتمدة (Message Templates) للتنبيهات المبادِرة — فهو مسموح للرسائل الصادرة المبنية على قوالب، لكنه لا يقرأ المجموعات. اختيار بين المرونة (Baileys) والأمان القانوني (Cloud API).

11) قائمة تحقّق للمبرمج (نقل الفكرة خطوة بخطوة)
 خدمة بوت Node منفصلة بـ Baileys + Express، تُدير Map<tenantId, Session> (جلسة لكل مدير).
 تخزين اعتماد مشفّر لكل جلسة في auth_sessions/<tenantId>/ (AES‑256‑GCM + HKDF).
 bootstrap() عند الإقلاع يُوقظ كل الجلسات المحفوظة، مع إعادة اتصال تلقائية.
 سرّ مشترك INTERNAL_API_KEY يحمي كل المسارات الداخلية بالاتجاهين (مقارنة timing‑safe).
 نقاط نهاية البوت: start, status, logout, delete, send, send-image, healthz — كلها تحت /sessions/:tenantId.
 كيان المدير (Tenant/Reseller) معزول، وtenant_id = مفتاح الجلسة؛ فلترة كل استعلام بالـ tenant.
 جدول الوكلاء يتبع المدير، وكل وكيل يحمل JID واتساب ورصيداً.
 قالب رسالة لكل مدير بعناصر نائبة ({AGENT}، {BALANCE}، {CURRENCY}) + معاينة حيّة في الواجهة.
 واجهة الربط: زر «ربط واتساب» + عرض QR + استفتاء الحالة كل ~2 ثانية حتى connected.
 زر إرسال فردي بجانب كل وكيل → يبني النص من القالب → send_message(tenant.id, agent.jid, text).
 زر إرسال جماعي لأصحاب الرصيد السالب → مهمّة خلفية بطابور + تأخير عشوائي ودفعات.
 احتياطات الحظر: رقم ثانوي/Business، تسخين، تحقّق onWhatsApp()، تنويع النص، معالجة 409.
 نشر: docker-compose بأربع خدمات (db/backend/bot/frontend) وتخزين دائم لمجلد auth_sessions.
12) خريطة ملفات مشروعنا (لو أراد المبرمج الرجوع للمصدر)
المكوّن	الملف عندنا
مدير الجلسات (جلسة لكل مدير)	bot/src/sessionManager.js
جلسة واتساب واحدة (QR، اتصال، رسائل)	bot/src/session.js
خادم البوت الداخلي (نقاط /sessions/...)	bot/src/server.js
عميل البوت ← Django (ingest/command)	bot/src/djangoClient.js
التخزين المشفّر للاعتماد	bot/src/encryptedAuthStore.js
عميل Django ← البوت (send/start/status)	backend/apps/accounts/bot_client.py
صلاحية السرّ الداخلي + تحميل الـ Tenant	backend/apps/ledger/permissions.py
نقاط الاستيعاب الداخلية (ingest/command)	backend/apps/ledger/views.py
الإرسال اليدوي (سابقة زر الرصيد)	backend/apps/ledger/views.py → send_reconcile
كيان المدير المعزول + القوالب	backend/apps/accounts/models.py → Tenant
واجهات الربط (connect/status/logout)	backend/apps/accounts/views.py
واجهة الربط (QR + polling)	frontend/src/pages/ConnectWhatsAppPage.tsx
تنظيم الخدمات والبيئة	docker-compose.yml
انتهى التقرير. الفكرة قابلة للتطبيق بالكامل: البنية نفسها التي نستخدمها للمطابقة تُغطّي حالتك (زر رصيد فردي + إرسال جماعي)، مع التشديد على العزل بالـ tenant وعلى احتياطات الحظر عند الإرسال الجماعي.